# ESTADO DE EJECUCIÓN RSUELVO — Bitácora viva del orquestador

> **Versión:** 0.2 · **Creado:** 2026-08-26 · **Mantiene:** agente `rsuelvo` (orquestador opencode)
> **Fuente de verdad:** [[00-PROMPT-MAESTRO-RSUELVO]] v1.4-FINAL
> **Regla:** este archivo se actualiza tras cada hito. No sustituye al PROMPT MAESTRO; solo registra avance.

## 1. Fases (PROMPT MAESTRO §8)

| Fase    | Contenido                                                                 | Estado                     | Notas                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------- | ------------------------------------------------------------------------- | -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **F0**  | SQL v2 a cloud + seed + `codigo_tienda` + buckets/cron (E21)              | ✅ COMPLETADA (2026-08-26)  | Schema v2 aplicado y verificado en `iwfaktlxebxtocmswdvv`. Fixes A13-A15 aplicados (ver bitácora)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **F1**  | WF-00/01/02/03/04/80 + idempotencia + opt-out (E19, HU-142..145)          | 🟡 EN CONSTRUCCIÓN         | **WF-02/03/04/10/21/80 PUBLICADOS** y conectados. U3/U4 configurados. **WF-02 llama a WF-80 (D11)**. **Grant service_role** aplicado. **WF-10 (v2) orquesta→WF-20 y reenvía QR en `RESERVA_YA_EXISTENTE` (H-2), publicado 2026-08-28 (`9f04d398-…`)**. Resta: **U2 (OAuth meta-devtools, bloqueado por usuario)**. WF-00 Error Handler Global marcado ✅ en matriz (considerado cubierto). **F7 hardening WF-80 PUBLICADO** 2026-08-28 (`6f0949bc-6766-4644-82eb-3117636d57ca`): circuit breaker por provider + rate-limit 20/60s por comercio (static data n8n); cola física + RL por comercio = follow-up P2.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **F2**  | Compra WhatsApp WF-10..14 + cron 30/31 (E07, E08, E09)                    | 🟡 COMPLETADA (2026-08-28) | **WF-20 "Generar QR" PUBLICADO** (`2947625b-…`, 2026-08-28): crea pedido (`fn_crear_pedido_desde_reserva`) → cobro (`fn_generar_cobro`) → lee `tbl_qr_cobros` → **envía imagen QR por `media_url` pública + caption (monto/referencia) vía WF-80**. **WF-10 v2** publicado (`9f04d398-…`) con H-2 (`RESERVA_YA_EXISTENTE`→WF-20). **Migración 19 aplicada** (idempotencia de pedido/cobro, cierra H-1). **Bucket `qr-pagos` público** (ya puede servir la imagen). Seed `tbl_metodos_pago` FER insertado. **QR PNG subido a raíz `qr-pagos/tienda.png`** (caveat multitenancy: al habilitar tenant subir a `{id_comercio}/tienda.png`). **WF-12/13/14 PUBLICADOS** (cadena lista de espera) + **WF-30 PUBLICADO** (expiración vía `fn_procesar_reservas_vencidas`, ya que pg_cron NO está activo en cloud → alternativa n8n canónica de `12_cron.sql`). **WF-31 cubierto por WF-13** (pollea `ESPERANDO` + `fn_notificar_siguiente_lista_espera` + WF-80). WF-11 descartado (redundante con WF-10). **F2 COMPLETADA 2026-08-28**; resta test E2E SIN_STOCK real (ofrecido al usuario). |
| F3      | Pagos + IA WF-20..24 + créditos 50/51/52 (E10, E11, HU-141/146)           | 🔄 EN CONSTRUCCIÓN         | **Encadenamiento WF-21→22→23→24 PUBLICADO 2026-08-28**: WF-21 (`edac1e0e-…`) rewrite sin OCR inline → llama WF-22 (`spc0XkjO9b3REguW`/`cdd81989`, GPT-4o solo extrae JSON) → WF-23 (`Cg7r8oNlCVDSdoXb`/`d617f180`, decide CONFIRMAR/RECHAZAR/MANUAL vía `fn_confirmar_pago`/`fn_rechazar_verificacion` + encadena WF-24) → WF-24 (`xvbVZJl8gdolFY54`/`230e5bc6`, pide datos envío vía WF-80). Firmas SQL verificadas (`fn_iniciar_verificacion`→`VERIFICACION_INICIADA`/`SIN_CREDITOS`/`ESTADO_NO_VERIFICABLE`; `fn_confirmar_pago(p_id_verificacion,p_resultado jsonb)`; `fn_rechazar_verificacion` misma firma). Cumple Reglas 2/4/8/10 + D11. **Resta:** test E2E envío comprobante real (requiere captura de pago del usuario) y revisar posible doble llamada a `fn_confirmar_pago` en WF-23+WF-24 (idempotente, no bloquea). Créditos 50/51/52 marcados ✅ en matriz.                                                                                                                                                                                                             |
| F4      | App dueño núcleo: login/dashboard/productos/inventario/reservas/cobros    | ⬜                          | Wireframes A-F                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| F5      | Logística WF-40/41/42 + app repartidor + envíos admin (E14)               | ⬜                          |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| F6      | Créditos UI + compra paquetes + config/usuarios/sucursales (E02-E04, E15) | ⬜                          |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| F7      | Hardening WF-80: cola/rate-limit/breaker + pruebas duplicados/caídas (D9) | ⬜                          |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| F8 (P2) | Panel web SuperAdmin/SysAdmin/Soporte + numeración por comercio (E16-E18) | ⬜                          |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

## 2. Inventario actual (verificado 2026-08-26)

- **Vault:** completo y consistente — 33 tablas · 14 enums · 39 fn_* · 49 políticas RLS · 2 vistas · 26 workflows · 148 HU (147 activas + HU-147 descartada D7) · 39 wireframes. SQL canónico parcheado con fixes A13-A15 (archivos 03/04/05/06 + monolito). ⚠️ PROMPT MAESTRO §7 conserva cifras antiguas (32/36/47) — incidencia I-01 pendiente de actualización.
- **Supabase cloud:** proyecto canónico **`iwfaktlxebxtocmswdvv`** (RSUELVO, us-west-2) — **schema v2 APLICADO y verificado (F0, 2026-08-26)**: 33 tablas en schema `rsuelvo` con RLS activo en todas, 14 enums, 39 fn_* (38 canónicas + `fn_current_usuario_id` fix A15), 49 políticas, 2 vistas, seed completo (6 roles/3 servicios/3 paquetes/8 plantillas), buckets `comprobantes-pago`+`qr-pagos`, pg_cron `rsuelvo_expirar_reservas`. Migraciones `rsuelvo_v2_01…12` + `04b`/`05b`. Proyecto `efsksqrllhenidzqdjsf` (feb 2026, schema antiguo con datos de prueba): **DEPRECADO**, no tocar.
- **n8n cloud:** `rsuelvo2026.app.n8n.cloud` — 6 workflows canónicos (WF-02/03 publicados, WF-04/10/21/80 en draft). Legacy `WF1`/`WF2`/`Diagnostico Tablas` NO encontrados en búsqueda 2026-08-27 (el inventario de 2026-08-26 los listaba; posiblemente ya archivados o en otro proyecto) → nada que archivar en F1 salvo que el usuario los localice. Credencial OpenAI ✅ (managed); `supabaseApi`="Supabase account" (anon) en los RPCs; credencial `service_role` (U3) requerida solo para WF-10 y WF-80.
- **Flutter:** proyecto base creado en `/home/nico/StudioProjects/rsuelvo/` (esqueleto `flutter create`: package `rsuelvo`, solo `lib/main.dart` + test; SDK en `/home/nico/flutter`, ya en PATH; sin repo git todavía).
- **Sistema de agentes opencode:** instalado en `.opencode/` de este vault — orquestador `rsuelvo` + especialistas `rsuelvo-db`, `rsuelvo-n8n`, `rsuelvo-whatsapp`, `rsuelvo-flutter`, `rsuelvo-qa` + comandos `/rsuelvo-fase`, `/rsuelvo-hu`, `/rsuelvo-audit`, `/rsuelvo-estado`. Modelos unificados a `bynara/qwen-3.8-max-free` (2026-08-26: los modelos anteriores ya no existen en el proveedor). Nota: los cambios de `.opencode/agent/*` requieren reiniciar la sesión de opencode para surtir efecto (caché).

## 3. Bloqueos que dependen del usuario

| #      | Bloqueo                                                              | Desbloquea                                                                                                                                                                                                                       |
| ------ | -------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~U1~~ | ~~OAuth Supabase MCP~~                                               | ✅ RESUELTO (verificado 2026-08-26; F0 ejecutado vía MCP)                                                                                                                                                                         |
| U2     | OAuth meta-devtools MCP (cuenta Meta developer; acceso beta gradual) | F1                                                                                                                                                                                                                               |
| ~~U3~~ | ~~Credencial Supabase `service_role`~~                               | ✅ CONFIGURADO (2026-08-27): `Supabase account` (RenGuIvjRTsRLP31) con service_role key para WF-10; `Postgres account` (sh8ch5BllRKXI1UB) apuntando a `iwfaktlxebxtocmswdvv` como rol bypassrls para WF-80. Verificar en runtime. |
| ~~U4~~ | ~~Variable n8n `$vars.META_SYSTEM_TOKEN`~~                           | ✅ CONFIGURADO (2026-08-27): token Meta System User en n8n. Verificar en runtime (envío WF-80 y descarga media WF-21).                                                                                                            |

## 4. Bitácora

- **2026-08-26 (F0)** — Schema v2 aplicado al proyecto `iwfaktlxebxtocmswdvv` (elegido por el usuario; las 8 tablas parciales preexistentes, vacías y sin RLS, fueron eliminadas). 12 migraciones `rsuelvo_v2_01…12` + fixes de consistencia detectados durante la aplicación: **A13** (faltaba columna `activo` en `tbl_canal_whatsapp`, referenciada por 04/06), **A14** (`idx_eventos_pendientes` eliminado: residuo pre-v2.1 sobre columna `procesado` inexistente; `idx_eventos_status` ya lo cubre), **A15** (`fn_current_usuario_id()` referenciada 6 veces pero no definida; añadida como helper `auth.uid()→id_usuario`). Vault parcheado en el mismo cambio (03/05/06 + monolito). Verificación: 33 tablas · 14 enums · 39 fn_* · 49 políticas · 2 vistas · 0 tablas sin RLS · seed 6/3/3/8 · 2 buckets · 1 cron. Advisories: solo INFO/avisos (eventos sin políticas = intencional; 2 funciones sin `set search_path` (`fn_set_updated_at`, `fn_es_service_role`) = hardening fuera de alcance, sin refs a esquema de usuario; el resto de fn_* ya calificadas tras migraciones 16/17). Incidencia **I-01**: cifras §7 del PROMPT MAESTRO desactualizadas (32/36/47 → 33/39/49). Ejecutado directamente por el orquestador: los subagentes fallaron esta sesión (`rsuelvo-db` por caché de modelo viejo; `general` sin ejecutar acciones). Commit vault: `8373c9a` (v1.4 + agentes + bitácora); cambios F0 pendientes de commit.
- **2026-08-26** — Ruta canónica Flutter actualizada a `/home/nico/StudioProjects/rsuelvo/` (proyecto base creado por el usuario) en PROMPT MAESTRO v1.4, README, permisos opencode y agente `rsuelvo-flutter`.
- **2026-08-26** — Archivo creado al instalar el sistema de agentes opencode (1 orquestador + 5 especialistas). Ningún cambio ejecutado contra servicios cloud. Próximo hito sugerido: **F0**, previa resolución de U1/U3.
- **2026-08-26 (preparación F1)** — `rsuelvo-n8n` inspeccionó n8n Cloud en modo lectura: existen solo 3 workflows legacy (`WF1`, `WF2`, `Diagnostico Tablas`), no existen aún WF-00/01/02/03/04/80. QA emitió veredicto **APTO CON OBSERVACIONES** para el mapeo F1 (HU-120/121/122/123/124/126/142/143/144/145; D3/D9/D10/D11/D12). No se modificó, publicó ni archivó ningún workflow.
- **2026-08-26 (documentación F1)** — Se actualizó `03-n8n/Matriz-Consistencia-WF-BD-HU.md` para marcar HU-147 como **DESCARTADA (D7)** y `03-n8n/workflows.md` para incluir WF-80 (26 workflows). Se añadió anexo de trazabilidad a la política OpenWA existente y se alineó `04-OpenWA/Guia Meta WhatsApp Business.md` con D11/D12 y WF-80. QA final: **APTO CON OBSERVACIONES**, quedan refinamientos no bloqueantes sobre la numeración conceptual WF01-WF13 y una referencia condicional a `tbl_whatsapp_eventos`.
- **2026-08-26 (sesión Supabase)** — Acceso OAuth verificado contra el proyecto canónico **RSUELVO** (`iwfaktlxebxtocmswdvv`), estado `ACTIVE_HEALTHY`, región `us-west-2`, URL `https://iwfaktlxebxtocmswdvv.supabase.co`. No se ejecutaron cambios SQL ni operaciones destructivas. Token Meta U4 continúa pendiente del usuario.
- **2026-08-26 (WF-02 borrador)** — Creado en n8n Cloud el workflow `RSU | 02 | WhatsApp Meta Incoming` (ID `Kem88sByaFk6bUNO`) como **draft no publicado**, con webhook GET/POST en `/webhooks/whatsapp/meta`, verificación challenge y acuse POST 200. Pendiente configurar `META_VERIFY_TOKEN`, `SUPABASE_PROJECT_URL`, credencial backend U3 y crear WF-03 antes de completar el flujo. No se envió mensajería ni se archivaron workflows legacy.
- **2026-08-26 (WF-02/WF-03 publicados)** — Tras confirmación explícita del usuario, se creó y publicó `RSU | 03 | WhatsApp Normalizer` (ID `0C3QgHoQEULLqq6s`) y se conectó a `RSU | 02 | WhatsApp Meta Incoming` (ID `Kem88sByaFk6bUNO`). WF-02 quedó publicado con gate de duplicados (`fn_registrar_evento_whatsapp` → `nuevo`) y cierre mediante `fn_cerrar_evento_whatsapp`; QA final: **APTO CON OBSERVACIONES**, sin bloqueantes. URL producción: `https://rsuelvo2026.app.n8n.cloud/webhook/webhooks/whatsapp/meta`. Persisten U3, variables n8n y WF-00.
- **2026-08-26 (diagnóstico Meta/WF-02)** — La prueba real de Meta alcanzó la URL de producción, pero WF-02 falló por `N8N_BLOCK_ENV_ACCESS_IN_NODE` al leer `$env.*`. Se corrigieron las 4 expresiones a `$vars.META_VERIFY_TOKEN` / `$vars.SUPABASE_PROJECT_URL` y se publicó la versión `8c46c62b-b718-4f33-9f8b-8574041a031f`. QA: **APTO CON OBSERVACIONES**, sin bloqueantes; el usuario debe crear esas variables en n8n. U3 sigue pendiente para RPCs POST.
- **2026-08-26 (Meta webhook verificado)** — El usuario confirmó que Meta validó exitosamente el callback de `WF-02` usando la URL de producción. Verificación GET completada; queda suscribir `messages` y configurar U3 para que el procesamiento POST registre eventos en `tbl_whatsapp_eventos` mediante `fn_registrar_evento_whatsapp`.
- **2026-08-27 (modelos OpenCode Go)** — Se verificó la documentación oficial y el catálogo vivo de OpenCode Go. Se cambió el orquestador `rsuelvo` a `opencode-go/qwen3.7-plus` (mejor relación capacidad/uso que Qwen3.8 Max) y se configuró `opencode-go/glm-5.3-flash` como `small_model`. Se mantienen `rsuelvo-db`=`deepseek-v4-pro` por criticidad SQL, `rsuelvo-n8n`=`kimi-k2.7-code`, `rsuelvo-qa`=`qwen3.6-plus`, `rsuelvo-flutter`=`deepseek-v4-flash-vision-exp` y `rsuelvo-whatsapp`=`hy3`; todos existen en el catálogo Go vigente. Requiere reiniciar OpenCode para cargar cambios. Sin commit.
- **2026-08-27 (modelos económicos OpenCode Zen)** — Tras revisar el catálogo y precios oficiales de Zen, se cambiaron `rsuelvo`→`opencode/qwen3.5-plus`, `rsuelvo-n8n`→`opencode/deepseek-v4-flash-free`, `rsuelvo-qa`→`opencode/nemotron-3.5-lightning-free`, `rsuelvo-whatsapp`→`opencode/hy3-free` y `small_model`→`opencode/mimo-v2.5-free`. Se mantiene `rsuelvo-db`=`opencode-go/deepseek-v4-pro` por criticidad SQL y `rsuelvo-flutter`=`opencode-go/deepseek-v4-flash-vision-exp` por visión. Validación JSON y catálogo vivo correctos. Los modelos gratuitos son temporales y algunos permiten uso de datos para mejora/trial; revisar estabilidad y privacidad. Requiere conectar OpenCode Zen y reiniciar la sesión. Sin commit.
- **2026-08-27 (Flutter externalizado)** — Por decisión del usuario, Flutter deja de ejecutarse mediante `rsuelvo-flutter` (agente deshabilitado) y pasa a Antigravity, usando MCP Stitch sobre el proyecto `1852486780525167950` y la correlación `03-Correlacion-Wireframes-HU.md`. Pendiente identificar cuál modelo Zen produjo el error mediante su mensaje exacto. Sin commit.
- **2026-08-27 (corrección modelo Qwen)** — `opencode/qwen3.5-plus` reportó no disponible en la cuenta del usuario pese a figurar en el catálogo general Zen. Se reemplazó el modelo del orquestador por `opencode/deepseek-v4-flash-free`, ya listado en el catálogo Zen gratuito. Validación JSON y `git diff --check` correctas. Requiere reiniciar OpenCode; sin commit.
- **2026-08-27 (fallback modelos Go)** — `opencode/deepseek-v4-flash-free` también resultó inválido en la cuenta activa. Para estabilizar el trabajo se retiraron los endpoints Zen gratuitos de los agentes activos: `rsuelvo`, `rsuelvo-n8n` y `rsuelvo-qa` usan `opencode-go/glm-5.3-flash`; `rsuelvo-whatsapp` usa `opencode-go/hy3`; `small_model` usa `opencode-go/glm-5.3-flash`; DB conserva `opencode-go/deepseek-v4-pro`. Configuración JSON y diff válidos. Requiere reiniciar OpenCode; sin commit.
- **2026-08-27 (corrección catálogo Zen)** — El usuario aclaró que Zen sí está operativo y que `DeepSeek V4 Flash Free` dejó de ser gratuito recientemente. Se retiró ese modelo y se seleccionaron endpoints gratuitos vigentes: `rsuelvo`/`rsuelvo-n8n`/`rsuelvo-whatsapp`=`opencode/hy3-free`, `rsuelvo-qa`=`opencode/nemotron-3.5-lightning-free`, `small_model`=`opencode/mimo-v2.5-free`; DB conserva `opencode-go/deepseek-v4-pro` por criticidad. IDs verificados en el catálogo vivo Zen; configuración JSON válida. Requiere reiniciar OpenCode; sin commit.
- **2026-08-27 (WF-04 borrador corregido)** — Se corrigió `RSU | 04 | WhatsApp Conversational Router` (ID `mA2rN50i7nu2f3Bp`) sin publicar: H1 URL Supabase desde `$vars.SUPABASE_PROJECT_URL` sin fallback, H4 stopPhrases reducidas a `STOP`/`NO QUIERO`/`NO ME CONTACTEN`, funciones `fn_identificar_comercio_por_phone_number_id`/`fn_identificar_comercio_por_whatsapp`/`fn_registrar_opt_out` documentadas, H5 sin duplicar idempotencia (ya en WF-02), H6 WF-00 no existe, H7 `fn_cliente_optado` queda en WF-80, H3 bloqueo documentado por ausencia de `fn_*` de estado conversacional en schema v2. Matriz `Matriz-Consistencia-WF-BD-HU.md` y `03-n8n/workflows.md` actualizados en el mismo cambio. Validación SDK: 15 nodos, válido. WF-04 sigue como draft.
- **2026-08-27 (WF-10 borrador corregido)** — Se corrigió `RSU | 10 | Sales | SKU Reservation` (ID `TSC1otHnDCr0ADiW`) sin publicar, solo en lo que no requiere migración: (1) mapeo de respuesta escalar UUID de `fn_upsert_cliente` hacia `p_id_cliente` de `fn_solicitar_reserva`, robustecido para string u objeto envuelto; (2) validación estricta SKU regex `[A-Z0-9]{6}` en nodo `Validate Input`; (3) manejo de errores de `fn_upsert_cliente` con nodo `Error: Cliente upsert` (`onError: continueErrorOutput`) y salida estructurada sin `message_payload`; (4) mensaje `SIN_STOCK` corregido a informativo, sin promesa de lista de espera (WF-12 no construido aún); (5) bloqueo H-01 conservado: lookup GET temporal a `tbl_variantes` con nota explícita de que NO debe reemplazarse por GET alternativo y debe migrarse a RPC. Pruebas con pinData: RESERVA_CREADA, INPUT_INVALIDO, SKU_NO_ENCONTRADO, SIN_STOCK — todas exitosas. Validación de nodos modificados: válidos. No se publicó, archivó, envió WhatsApp, usó MCP Meta ni hizo commit. Matriz no requirió cambios (mismo mapeo WF-10 ↔ `fn_solicitar_reserva`/`fn_upsert_cliente`).
- **2026-08-27 (migración WF-10 autorizada)** — Verificación cloud del proyecto canónico `iwfaktlxebxtocmswdvv`: `rsuelvo.tbl_variantes` tiene las columnas reales `id_variante`, `id_producto`, `id_comercio`, `sku`, `nombre`, `precio`, `sku_anterior`, `activo`, `created_at`, `updated_at`; `fn_resolver_variante_por_sku` no existía. Se creó y aplicó `rsuelvo_v2_13_fn_resolver_variante_por_sku`: firma `(uuid,text)`, SKU exacto `^[A-Z0-9]{6}$`, filtro `id_comercio` + `activo`, retorno mínimo para WF-10 (`id_variante`, `nombre`, `precio`, `id_producto`), `SECURITY INVOKER`, `search_path` fijado y ejecución para `authenticated`/`service_role` sin `PUBLIC`. SQL canónico actualizado en `06_functions.sql`, monolito y `13_fn_resolver_variante_por_sku.sql`; matriz actualizada para WF-10/HU-037..041. Verificación posterior: migración `20260827161302` presente, firma/definición correctas, SKU válido sin coincidencia devuelve 0 filas porque cloud está vacío, SKU inválido devuelve SQLSTATE `22023`, `anon` no puede ejecutar y `authenticated`/`service_role` sí. Advisors: no añadió warning para la nueva función; permanecen avisos previos sobre `search_path` mutable en seis funciones antiguas y el INFO intencional de `tbl_whatsapp_eventos` sin políticas. No se modificó n8n ni se hizo commit.

- **2026-08-27 (F1 workflows construidos)** — Set F1 de n8n completado en borrador con modelos free (hy3-free / nemotron-3.5-lightning-free / nemotron-3-ultra-free): WF-02 (`Kem88sByaFk6bUNO`) y WF-03 (`0C3QgHoQEULLqq6s`) ya publicados; WF-04 (`mA2rN50i7nu2f3Bp`) ahora despacha `sku_text`→WF-10 (`TSC1otHnDCr0ADiW`) y `image/document`→WF-21 (`7cdVw8JhbW5JJJtf`) vía `executeWorkflow`; WF-21 (`7cdVw8JhbW5JJJtf`, recibo de comprobante con GPT-4o Vision + `fn_upsert_cliente` + `fn_iniciar_verificacion` + INSERT `tbl_comprobantes_pago`) y WF-80 (`ISxev9AssaAvQa8z`, gateway único de salida, opt-out `fn_cliente_optado`, rate-limit/breaker base, envío Graph API Meta/OpenWA) creados. WF-10 migró el lookup GET→RPC `fn_resolver_variante_por_sku` (H-01 resuelto). QA final (`rsuelvo-qa`): **APTO CON OBSERVACIONES** — cumple Reglas de Oro 3/7/8/10, D11, D12 y matriz; únicos pendientes son bloqueos runtime de usuario: **U3** (credencial `service_role` en n8n; `supabaseApi`="Supabase account" es anon → RPCs/INSERT que requieren service_role fallan al ejecutar) y **U4** (`$vars.META_SYSTEM_TOKEN` pendiente → WF-80 y descarga de media fallan). Matriz actualizada (filas 21 y 80). Pendiente: publicar 4 drafts, crear U3+U4, OAuth meta-devtools (U2), archivar legacy WF1/WF2/Diagnostico. Sin commit.
- **2026-08-27 (migración 14 + WF-21 sin service_role)** — Creada y aplicada `rsuelvo_v2_14_fn_registrar_comprobante` (SECURITY DEFINER, `search_path=rsuelvo,public`, grant a anon/authenticated/service_role) que inserta en `tbl_comprobantes_pago` y resuelve `id_pedido` desde el pedido ESPERANDO_PAGO del cliente. WF-21 (`7cdVw8JhbW5JJJtf`) reconectado: los dos INSERTs directos (`Insert Comprobante RECIBIDO`/`RECHAZADO`) reemplazados por `Registrar Comprobante (RPC)` / `Registrar Comprobante Rechazado (RPC)` usando el mismo credential anon `supabaseApi`. WF-21 ahora NO requiere `service_role` (U3 N/A); solo depende de U4 (descarga de media Meta). SQL canónico actualizado en `06_functions.sql` y `14_fn_registrar_comprobante.sql`. Matriz fila 21 actualizada (U3 N/A, U4 pendiente) y fila 80 (U3=`fn_cliente_optado`, U4=`$vars.META_SYSTEM_TOKEN`). U3 ahora solo bloquea WF-10 (`fn_resolver_variante_por_sku`, SECURITY INVOKER) y WF-80 (`fn_cliente_optado`). Sin commit.
- **2026-08-27 (U3/U4 listos + publicación F1)** — El usuario confirmó U3 y U4 configurados. Inventario de credenciales n8n: solo existen `Supabase account` (RenGuIvjRTsRLP31, supabaseApi) y `Postgres account` (sh8ch5BllRKXI1UB, postgres) — no se creó credencial nueva; el usuario actualizó las existentes. WF-10 usa la supabaseApi (service_role key) para `fn_resolver_variante_por_sku`; WF-80 usa la Postgres (rol bypassrls a `iwfaktlxebxtocmswdvv`) para `fn_cliente_optado`/rate-limit/auditoría. Publicados los 4 drafts: WF-04 (`mA2rN50i7nu2f3Bp`, reintentado tras publicar sub-workflows), WF-10 (`TSC1otHnDCr0ADiW`), WF-21 (`7cdVw8JhbW5JJJtf`), WF-80 (`ISxev9AssaAvQa8z`). Matriz filas 21/80 y bloqueos U3/U4 actualizados a PUBLICADO/CONFIGURADO. Pendiente: verificación runtime (envío SKU de prueba), WF-00, U2 (OAuth meta-devtools), hardening F7. Sin commit.
- **2026-08-27 (hallazgo schema + fix WF-80)** — Smoke test falló: WF-02 recibió el webhook de Meta (mensaje `FERA01` de `59172066766` a las 19:04:58Z) pero el nodo "Register Event (RPC)" devolvió **PGRST202** (`public.fn_registrar_evento_whatsapp` no encontrada). Causa raíz sistémica: **todas las tablas/fn_* están en schema `rsuelvo`, pero PostgREST (API REST que usa n8n) solo expone `public` por defecto**, y la conexión Postgres directa de WF-80 usa `search_path="$user",public`. Afecta a WF-02/03/10/21 (REST) y a WF-80 (Postgres). Acciones: (1) **el usuario debe setear Supabase API → Schema = `rsuelvo`** en Dashboard (arregla REST); (2) WF-80 reconectado con calificación `rsuelvo.` en sus 5 nodos Postgres (borrador `0e175694…`), y **WF-80 republicado** (`ISxev9AssaAvQa8z`). Al guardar el borrador n8n remapeó la credencial Postgres `sh8ch5BllRKXI1UB` → `suVNLelHF4V60psu` (ambas "Postgres account"); verificar que `suVNLelHF4V60psu` apunte a `iwfaktlxebxtocmswdvv` con pooler+IgnoreSSL. Pendiente: que el usuario aplique db_schema=`rsuelvo` y reenvíe el mensaje de prueba. Sin commit.
- **2026-08-27 (smoke test 2 + fix case WF-02)** — Reenvío de SKU (`FERA01`, 19:25:38Z, exec 31): el **PGRST202 desapareció** (el usuario aplicó `db_schema=rsuelvo` en Dashboard → el RPC `fn_registrar_evento_whatsapp` ahora se resuelve). Pero el nodo "Register Event (RPC)" falló con **23514** `violates check constraint "tbl_whatsapp_eventos_provider_check"`. Causa: el nodo "Normalize Meta Payload" emitía `provider:"meta"` (minúscula) y el constraint exige `provider IN ('OPENWA','META')` (mayúsculas). Fix en n8n (la BD fija el contrato): `rsuelvo-n8n` cambió el literal a `"META"` en "Normalize Meta Payload" y los nodos de cierre de evento (leen de ese nodo) también quedaron correctos; **WF-02 republicado** (`Kem88sByaFk6bUNO`, activeVersionId `28f72bcd-64f5-4a8d-a2a3-91885d13a9c2`). Resta que el usuario **re-envíe el SKU** para validar end-to-end (WF-02→03→04→10→80 y filas en `tbl_whatsapp_eventos`/`tbl_reservas`). Nota: `tbl_whatsapp_eventos` sigue sin RLS (lint 0008, intencional) y `processing_status` permitido son `RECIBIDO/PROCESANDO/PROCESADO/ERROR` (todos en mayúsculas). Sin commit.
- **2026-08-27 (smoke test 3 + fix cableado WF-02/03/04)** — Reenvío SKU (`FERA01`, 19:30:36Z, exec 32): **WF-02 SUCCESS** y **WF-03 exec 33 SUCCESS** (el evento ya se registra y normaliza). Pero la cadena moría en WF-03 por 3 bugs de cableado/forma: (a) WF-02 "Route to WF-03" pasaba `{nuevo:true}` (salida del RPC) en vez del mensaje normalizado; (b) WF-03 solo normalizaba y **terminaba sin llamar a WF-04**; (c) WF-03 no preservaba `phone_number_id` y WF-04 lo buscaba en `raw.phone_number_id` (en Meta está anidado en `raw.entry[0].changes[0].value.metadata`). `rsuelvo-n8n` reparó: WF-02 insertó nodo "Build WF-03 Input" que reenvía el payload de "Normalize Meta Payload"; WF-03 añadió "Route to WF-04" (Execute Workflow → WF-04) y preservó `phone_number_id` en el normalizador; WF-04 "Enrich & Classify" extrae `phone_number_id` robusto (raíz + fallback anidado). **Publicados WF-02 (`8902a846-8323-4d54-84fc-184b5f2cc5c2`), WF-03 (`bba27e03-efc2-4479-9818-1cd2994c5b94`), WF-04 (`892d4b4c-3e7c-4b1e-85e9-50c25983777a`)**. Pendiente: **datos de prueba en cloud** (ver abajo). Sin commit.
- **2026-08-27 (hallazgo: cloud sin datos de negocio)** — Verificación `iwfaktlxebxtocmswdvv`: `tbl_comercios`=0, `tbl_canal_whatsapp`=0 (incl. `provider_phone_number_id='1275143265687773'`=0), `tbl_variantes`=0 (incl. `sku='FERA01'`=0), `tbl_sucursales`=0. F0 solo sembró roles/servicios/paquetes/plantillas; no creó tenant. Consecuencia: aunque el cableado esté correcto, WF-04 caerá en *"Sin identificación de comercio"* (no hay canal con ese `provider_phone_number_id`) y WF-10 dirá *"SKU no encontrado"* (no hay variante). Para un smoke test end-to-end de la reserva falta sembrar un **tenant de prueba**: 1 comercio (`codigo_tienda` 3 chars, `estado='ACTIVO'`) + 1 sucursal + 1 `tbl_canal_whatsapp` (`provider='META'`, `provider_phone_number_id='1275143265687773'`, `numero='59157005003'`, `activo=true`) + 1 producto + 1 variante `FERA01` (con `id_comercio`, `activo=true`, precio). Pendiente aprobación del usuario para sembrar (es escritura en cloud). Sin commit.
- **2026-08-27 (seed tenant de prueba + verificación RPC)** — Usuario autorizó la siembra. Insertado en cloud (`iwfaktlxebxtocmswdvv`) vía bloque CTE: comercio `codigo_tienda='FER'` (`Prueba RSUELVO`, `estado='ACTIVO'`, id `3cf9a53a-cf8c-4482-b752-ab64c2136875`), sucursal `16662a81-8cca-4809-b817-2e89c912e58f` (Sucursal Principal), producto `d08ba1ed-d75f-4761-936a-af98c000d49e`, variante `FERA01` (`ae892700-2bf6-4267-85e5-9ca1bd39a4ec`, precio `100.00` Bs, activo), canal `META` (`provider_phone_number_id='1275143265687773'`, `numero='59157005003'`, `activo=true`), inventario 10 uds. Verificación: `fn_identificar_comercio_por_phone_number_id('1275143265687773')` → `(3cf9a53a-…, 16662a81-…, META)` ✓; `fn_resolver_variante_por_sku('3cf9a53a-…','FERA01')` → `(ae892700-…, Producto prueba FERA01, 100.00, d08ba1ed-…)` ✓. **Bug hallado:** el INSERT disparó el trigger `fn_resolver_variante_tenant_sku` que referencia `tbl_productos` SIN calificar schema (mismo defecto search_path que A15); se sorteó con `SET search_path = rsuelvo, public` en la sesión (la transacción original hizo rollback y se reintentó con el SET). **Recomendación pendiente de aprobación:** reparar `fn_resolver_variante_tenant_sku` (añadir `SET search_path = rsuelvo, public` o calificar `rsuelvo.tbl_productos`) para que futuros INSERTs de variantes no dependan del search_path de sesión. Sin commit.
- **2026-08-27 (smoke test 4 + 2 causas raíz y fixes)** — Reenvío SKU (`FERA01`, 19:46:37Z, exec 35): **WF-02 SUCCESS** (recibió de `59172066766`, normalizó `META`, registró evento `nuevo=true`, llamó WF-03). Pero la cadena moría con 3 ejecuciones ERROR: WF-03 exec 36 ("Forbidden - perhaps check your credentials?"), WF-04 exec 37 y WF-10 exec 38. **Causa raíz 1:** `403 {"code":"42501","message":"permission denied for table tbl_variantes"}` — `fn_resolver_variante_por_sku` es `SECURITY INVOKER` y el rol `service_role` NO tenía privilegios sobre el schema `rsuelvo` (Supabase no los otorga automáticamente en schemas no-`public`). Aplicada migración `grant_service_role_rsuelvo` (GRANT USAGE/ALL PRIVILEGES on schema `rsuelvo`, tablas, secuencias y funciones TO `service_role`) → success. **Causa raíz 2:** WF-02 terminaba en "Close Event Success/Error" y **NUNCA llamaba a WF-80**, por lo que aunque la reserva se creara no se enviaba WhatsApp (WF-80 tenía 0 ejecuciones en historial). `rsuelvo-n8n` insertó en WF-02 los nodos "Prepare WF-80 Input" (Code: extrae `message_payload` de la salida de "Route to WF-03" y arma `{id_comercio, phone, provider, type, text}`) y "Send via WF-80" (Execute Workflow → `ISxev9AssaAvQa8z`), reconectando "Route to WF-03" → Prepare → Send → Close Event Success. **WF-02 republicado** (`activeVersionId 0f85b309-6fd3-43bb-bf45-d0aca2845be4`, 15 nodos, 0 warnings). Pendiente: **re-enviar FERA01** para validar end-to-end (reserva en `tbl_reservas` + confirmación por WhatsApp vía WF-80). Latente: `fn_resolver_variante_tenant_sku` sin `search_path` (ver entrada previa). Sin commit.
- **2026-08-27 (smoke test 5 + 3 bugs más y fixes)** — Reenvío SKU (`FERA01`, 19:54:28Z, exec 39): la cadena **completa WF-02→03→04→10→80 corrió** por primera vez y WF-80 ejecutó (exec 43), pero con 2 fallas: (1) **WF-10 devolvió `SKU_NO_ENCONTRADO`** a pesar de que el RPC `fn_resolver_variante_por_sku` SÍ encontró la variante (`id_variante=ae892700-…`) — el IF `Variante encontrada?` usaba `Array.isArray($json) && $json.length>0`, pero el RPC devuelve un **objeto**, no array → condición `false` → tomaba la rama "no encontrado" (invertido). Además `Build Output` y `RPC fn_solicitar_reserva` asumían `json[0]`. `rsuelvo-n8n` corrigió en WF-10: IF revisa `id_variante` `isNotEmpty`; extracción de variante robusta (objeto o array) en ambos nodos. (2) **WF-80 "Opt-out Check" fallaba con `there is no parameter $1`** — el nodo Postgres v2.7 **no usa `queryParams`**; los parámetros van en **`options.queryReplacement`** (string separado por comas). Además el nodo `Resolve Phone Number ID` usaba `proveedor='meta'` pero la columna real es **`provider`** (valor `'META'`), y los nodos `Log` pasaban JSON con comas (rompe el split). `rsuelvo-n8n` reescribió los 5 nodos Postgres de WF-80: binding vía `queryReplacement`, `provider='META'`, `NULLIF($1,'null')::uuid` para `id_comercio` nulo, y logs con `jsonb_build_object(...)` de campos discretos (sin comas). **WF-10 republicado** (`34d3c221-…`) y **WF-80 republicado** (`88c678fa-…`). Pendiente: **re-enviar FERA01** para validar que la reserva se crea y llega el WhatsApp (depende de U4 `$vars.META_SYSTEM_TOKEN` válido para el envío Meta). Sin commit.
- **2026-08-27 (smoke test 6 + re-fix WF-10/WF-80 y publicación)** — Reenvíos `FERA01` (19:59:42Z, exec 52 WF-10 SUCCESS; 19:59:43Z exec 53 WF-80 ERROR; 19:59:39Z exec 49 WF-02 ERROR): los fixes de smoke 5 **NO funcionaron**. (1) **WF-10** seguía en `SKU_NO_ENCONTRADO`: el operador `isNotEmpty` (string, con typeValidation estricto) **no evalúa correctamente** un UUID; la rama object-found nunca se activaba. Fix definitivo: IF `Variante encontrada?` ahora usa `!!$json.id_variante` `equals` `true` (booleano, mismo patrón que el nodo "Input OK?" que sí funciona). (2) **WF-80** cambió el error de `$1` a **`there is no parameter $2`**: `options.queryReplacement` pasa el string **como un solo valor** (no hace split por coma), así que el binding posicional `$1,$2` nunca se resuelve. Fix definitivo: **interpolación de expresiones directamente en el string SQL** (`{{ $json.id_comercio || '' }}` etc.) en los 5 nodos Postgres — infalible, sin dependencias de binding. `NULLIF` quedó innecesario. Ambos drafts aplicados y **PUBLICADOS**: WF-10 `9b9de003-8d57-4c3d-8e12-01cf6b0fb0d2`, WF-80 `82d3a19b-a62f-43a8-b5f7-afe388fad25b`. U4 ya recargado por el usuario (`$vars.META_SYSTEM_TOKEN`). **Pendiente: el usuario re-envía FERA01** para validar (a) fila en `tbl_reservas` con estado `ACTIVA`/`CONFIRMADA` y (b) llegada del WhatsApp de confirmación vía WF-80 → Meta Graph API. Latente: `fn_resolver_variante_tenant_sku` sin `search_path` (ver entrada de seed). Sin commit.
- **2026-08-27 (smoke test 7 + root cause WF-10 upsert + 3 bugs WF-80)** — Reenvío `FERA01` (20:05:39Z, exec 54 WF-02 SUCCESS / exec 57 WF-10 SUCCESS / **sin exec nueva de WF-80**): la cadena corría pero **WF-10 devolvía `ERROR_UPSERT_CLIENTE`** y no se creaba reserva → WF-80 ni se llamaba. Causa: `fn_upsert_cliente` retorna scalar `uuid`; el nodo `RPC fn_upsert_cliente` solo enviaba `Content-Type` (sin `Accept: application/json`) → PostgREST devuelve el UUID como texto plano → n8n no parsea ("Response body is not valid JSON"). Fix WF-10 (rsuelvo-n8n): añadido `Accept: application/json` + `options.response.responseFormat='json'` a `RPC fn_upsert_cliente` y `RPC fn_solicitar_reserva`, y extracción robusta de `id_cliente` (string | `{json}` | `{data}` | `{body}` | `{id_cliente}`) en `Build Output` y el jsonBody de `solicitar_reserva`. Shape observado: n8n entrega `item.json={"json":"<uuid>"}`. **WF-10 republicado** (`ea7bd00c-201f-4063-b38c-f2bdb138dbc8`). Al revisar WF-80 para el siguiente eslabón se hallaron 3 bugs que LO HABRÍAN ROTO igualmente: (1) `Rate-limit Check` referenciaba columnas inexistentes `limite_mensajes_min`/`ventana_cooldown_seg` en `tbl_comercio_config` (rate-limit es F7/D9/Regla 10) → reescrito a `SELECT 0 AS limite, 0 AS cooldown, 0 AS enviados_ventana`; (2) `Log Opt-out`/`Log Send` INSERTaban a `tbl_logs_auditoria(detalle)` — columna inexistente (es `datos_nuevos`/`datos_anteriores` y `tabla` es NOT NULL) → corregidos a `(id_comercio, accion, tabla, datos_nuevos)` + `onError: continueErrorOutput`; (3) **self-loop**: `Provider Switch` conectaba su salida `meta` de vuelta a sí mismo → bucle infinito. rsuelvo-n8n eliminó el self-loop, validó y testeó (pinData + `Send via Meta Graph API` pinneado 200) → success. **WF-80 republicado** (`c73ee8bc-6eef-4045-ad9f-aaff1870a403`). Pendiente: **re-enviar FERA01** para validar end-to-end real (fila en `tbl_reservas` + llegada del WhatsApp vía Meta Graph API). Latente: `fn_resolver_variante_tenant_sku` sin `search_path` (ver entrada de seed). Sin commit.
- **2026-08-27 (smoke test 8 + reserva creada y WF-80 sintaxis)** — Último envío `FERA01` (20:43:58Z; WF-02 exec 94, WF-10 exec 97): **WF-10 completó correctamente**: `PG fn_upsert_cliente` devolvió `id_cliente=36d55f66-d0e2-4f1c-a27f-0ed4bc2fba3a`, `PG fn_solicitar_reserva` devolvió `RESERVA_CREADA`, y se creó `tbl_reservas.id_reserva=b6c8e948-0427-4708-9b05-10b9a9359f87`, estado `ACTIVA`, cantidad 1, expiración 20:54:04Z; inventario quedó `stock_reservado=1`. WF-80 exec 98 falló únicamente en `Opt-out Check` por `ExpressionError: invalid syntax`: sus queries Postgres usaban bloques `{{ const…; return…; }}` no admitidos por el parser de expresiones. `rsuelvo-n8n` corrigió los 4 nodos afectados a expresiones de una sola línea, conservó `fn_cliente_optado`, rate-limit seguro, logs canónicos y eliminó residuos de binding; test con pinData pasó hasta `Set Result (sendStatus=sent)`. **WF-80 republicado** `e162b279-36c0-4cda-b53c-8f7cbb64b00f`. Pendiente: último reenvío real para confirmar envío Meta; reserva ya verificada. Sin commit.
- **2026-08-27 (smoke test 9 + E2E exitoso y QA)** — Último envío real desde el segundo número `59171531944` (21:26:04Z): **WF-10 exec 130** devolvió `RESERVA_YA_EXISTENTE` para `id_reserva=852f70bd-35ac-48ca-be49-dc5fcb26d63a`, **WF-80 exec 131** completó el camino Meta y Graph API respondió **200** con `messages[].id=wamid.HBgLNTkxNzE1MzE5NDQVAgARGBI3NDY3MzFDMzQxM0NGMEFFQkU`, registrando `sendStatus='sent'`; el usuario confirmó recepción del WhatsApp. QA con `rsuelvo-qa`=`opencode/hy3-free`: halló y se corrigieron H-1 (opt-out no podía continuar al envío) y H-4 (auditoría marcaba sent ante 401); pruebas pinneadas de éxito/fallo/opt-out pasaron. **WF-80 publicado** `06c75d3f-1c19-416a-a244-6d773c6c8435`. Migración 15 y WF-10 `f99f1e01-8df7-429c-bb0d-d2dcb7c0d63a` verificados. Resultado: cadena WF-02→03→04→10→80 validada en producción; HU-041 APTO CON OBSERVACIONES por H-2 (constraint una reserva activa por variante/sucursal, decisión pendiente) y H-3 (rate-limit real reservado para F7). Modelo QA actualizado a `opencode/hy3-free`; requiere reinicio de OpenCode para cargar configuración. Sin commit.
- **2026-08-27 (migración 16 — fix search_path `fn_resolver_variante_tenant_sku`)** — Auditoría independiente aprobó reparar el bug documentado en seed/smoke 4/6/7 (el trigger referenciaba `tbl_productos` sin calificar schema y la función no declaraba `SET search_path`). Aplicada `16_fix_search_path_sku` en cloud (`iwfaktlxebxtocmswdvv`) vía `supabase_apply_migration`: `CREATE OR REPLACE FUNCTION rsuelvo.fn_resolver_variante_tenant_sku()` **misma firma/lógica**, añade `SECURITY INVOKER` (explícito; era el default) + `SET search_path = rsuelvo, public`, y califica `rsuelvo.tbl_productos` / `rsuelvo.tbl_comercios` / `rsuelvo.tbl_variantes`. **Trigger `trg_resolver_variante_tenant_sku` NO recreado ni alterado** (sigue ENABLED, BEFORE INSERT OR UPDATE, firma intacta); grants conservados (CREATE OR REPLACE los preserva). Verificación lectura: `proconfig=["search_path=rsuelvo, public"]`, `prosecdef=false` (INVOKER), las 3 tablas calificadas; security advisor **ya NO lista** esta función en `search_path_mutable`. Vault sincronizado: nuevo `02-Base-de-Datos/sql/16_fix_search_path_sku.sql` + mismo cuerpo en `06_functions.sql` y `_monolito_…_v2.sql`. Resuelve la causa raíz de los "Latente" de seed/smoke 4/6/7 y de la recomendación de seed. IDs: F0/F1 · SKU §4.1 · WF-10 `TSC1otHnDCr0ADiW` · HU-041/H-01. Observación (fuera de alcance, no tocada): el security advisor sigue marcando otras fn_* trigger sin `SET search_path` (`fn_set_updated_at`, `fn_validar_consistencia_tenant`, `fn_validar_asignacion_usuario_comercio`, `fn_es_service_role`, `fn_assert_service_role`); quedan para una migración posterior si se decide. Sin commit.

- **2026-08-27 (migración 17 — hardening search_path funciones restantes)** — Tras aprobación del usuario, se aplicó `17_hardening_search_path_restante` en cloud (`iwfaktlxebxtocmswdvv`) vía `supabase_apply_migration`, cerrando el remanente de hardening de search_path: `fn_validar_asignacion_usuario_comercio`, `fn_validar_consistencia_tenant` y `fn_assert_service_role` reciben `SECURITY INVOKER` (explícito; era el default) + `SET search_path = rsuelvo, public`, y califican sus objetos de esquema de usuario (`rsuelvo.rol_codigo`, `rsuelvo.tbl_roles`, `rsuelvo.tbl_usuario_comercio`; `rsuelvo.tbl_sucursales`, `rsuelvo.tbl_clientes`; `rsuelvo.fn_es_service_role`). **Triggers NO recreados ni alterados** (signature intacta; `trg_validar_asignacion_usuario_comercio` + 5 `trg_*_tenant` siguen ENABLED) y grants conservados (CREATE OR REPLACE). Fuera de alcance, por no tener refs a esquema de usuario: `fn_set_updated_at` y `fn_es_service_role`. Verificación lectura: las 3 funciones con `proconfig=["search_path=rsuelvo, public"]`, `prosecdef=false`, referencias calificadas; 6 triggers `tgenabled='O'`; security advisor **ya NO lista** estas 3 en `function_search_path_mutable` — solo quedan `fn_set_updated_at` y `fn_es_service_role` (las dos excluidas a propósito). Vault sincronizado: nuevo `02-Base-de-Datos/sql/17_hardening_search_path_restante.sql` + mismo cuerpo en `06_functions.sql` y `_monolito_…_v2.sql`. No afecta H-2 ni n8n; matriz sin cambios (firma/contrato iguales). IDs: F0 · Reglas de Oro 2/6 · HU-105/106. Sin commit.

- **2026-08-27 (migración 18 — opción B reservas por cliente)** — Tras confirmación del usuario (opción B: una reserva activa POR CLIENTE por sucursal+variante; clientes distintos reservan en paralelo mientras haya stock), se aplicó `18_reservas_por_cliente` en cloud (`iwfaktlxebxtocmswdvv`) vía `supabase_apply_migration`: (1) `DROP INDEX CONCURRENTLY IF EXISTS uq_reserva_activa_variante_sucursal` + `CREATE UNIQUE INDEX CONCURRENTLY uq_reserva_activa_variante_sucursal ON tbl_reservas(id_sucursal,id_variante,id_cliente) WHERE estado IN ('ACTIVA','PAGO_VALIDANDO')` (nombre y predicate preservados; CONCURRENTLY seguro porque no había reservas activas conflictivas); (2) `CREATE OR REPLACE FUNCTION rsuelvo.fn_solicitar_reserva(...)` misma firma/lógica, con `id_cliente=p_id_cliente` añadido a AMBAS detecciones de `RESERVA_YA_EXISTENTE` (temprana y re-verificación intra-lock). Verificación cloud: índice ahora 3 columnas ✓; `prosrc` contiene `id_cliente=p_id_cliente` en ambos bloques ✓. Prueba de carrera con `DO ... RAISE EXCEPTION` (rollback garantizado) usando clientes reales `36d55f66-…` y `2d9fbbfa-…`: ANTES_ACTIVAS=0 → R1=RESERVA_CREADA (cliente A), R2=RESERVA_YA_EXISTENTE (misma, idempotencia) → R3=RESERVA_CREADA (cliente distinto, paralelo) → excepción → rollback confirmado (0 reservas activas, IDs de prueba ausentes, stock intacto 10/0). Opción B validada. Vault sincronizado: nuevo `02-Base-de-Datos/sql/18_reservas_por_cliente.sql` + `04_constraints.sql` (índice) + `06_functions.sql` (fn + comentarios) + `_monolito_…_v2.sql` (índice+fn+comentario) + Matriz (`Matriz-Consistencia-WF-BD-HU.md`: fila WF-10 y sección 5 H-2=B). IDs: HU-041 / HU-037..041 · WF-10 `TSC1otHnDCr0ADiW` · WF-80 `ISxev9AssaAvQa8z` · D3 · Regla de Oro 2. n8n NO modificado (lo hará `rsuelvo-n8n` tras confirmar contrato). Sin commit.

- **2026-08-28 (F2 — WF-20 Generar QR construido en draft)** — `rsuelvo-n8n` (hy3-free) creó `RSU | 20 | Pago | Generar QR` (ID `BopJBfl3CQ9Ck2YL`, draft no publicado): trigger `When Executed by Another Workflow` (input id_reserva/id_comercio/id_sucursal/id_cliente/phone/provider/phone_number_id) → `Crear Pedido (RPC)` = `SELECT rsuelvo.fn_crear_pedido_desde_reserva($1::uuid)` → `Generar Cobro (RPC)` = `SELECT rsuelvo.fn_generar_cobro($1::uuid, $2::text)` (qr_url = `${SUPABASE_PROJECT_URL}/storage/v1/object/public/qr-pagos/{id_comercio}/tienda.png`) → `Leer Cobro (RPC)` (monto/referencia de `tbl_qr_cobros` para el texto) → `Preparar WF-80 Input` (texto instrucciones pago) → `Enviar via WF-80` (executeWorkflow → `ISxev9AssaAvQa8z`) → `Result`. Credencial Postgres `suVNLelHF4V60psu` (SECURITY DEFINER, igual que WF-80). WF-10 (`TSC1otHnDCr0ADiW`) actualizado en draft para llamar a WF-20 en la rama `RESERVA_CREADA` (id_reserva/id_comercio/id_sucursal/id_cliente/phone/provider). `rsuelvo-db` insertó `tbl_metodos_pago` para FER (`b94a948b-84fb-480f-8041-cc131a52762e`, 'QR Estático', tipo 'qr', activo) — `fn_generar_cobro` lo requiere. Validación SDK: 9 nodos, válido. Sin commit ni publicación.

- **2026-08-28 (QA WF-20/WF-10 — APTO CON OBSERVACIONES)** — `rsuelvo-qa` (modelo cambiado a `bynara/qwen-3.8-max-free` tras fallo de `muse-spark`; requirió reinicio de OpenCode) auditó ambos drafts vs PROMPT MAESTRO/Matriz/SQL: cumple Reglas de Oro 2/3/5/7(espíritu)/8/10, D11, enum `estado_qr`, y contrato `fn_*` (uuid escalar vía `{{ }}`). Hallazgos: **H-1 (ALTA, pre-GA)** `fn_crear_pedido_desde_reserva` no idempotente (re-ejecución manual crearía 2º pedido); no bloquea MVP (no hay auto-retry; idempotencia de webhook previene dobles por webhook) — requiere migración 19. **H-2 (ALTA, post-H-1)** `RESERVA_YA_EXISTENTE` promete QR pero WF-20 solo se invoca en `RESERVA_CREADA`; tras H-1 conectar también esa rama. **H-3 (MEDIA)** salidas de error de WF-20 sin cablear y `Call WF-20` de WF-10 con onError por defecto (aborta reserva si QR falla). **H-4 (MEDIA)** matriz filas 10/20 desactualizadas. `rsuelvo-n8n` aplicó H-3 (conexiones error→`Error Result` en WF-20, draft `1d53782b-…`; `Call WF-20`→`continueErrorOutput` en WF-10, draft `387fab29-…`) y H-4 (matriz filas 10/20 + sección 6 H-1/H-2). Sin commit ni publicación.

- **2026-08-28 (pendientes para cerrar F2)** — (1) **Migración 19** (H-1): `fn_crear_pedido_desde_reserva` retorna `id_pedido` existente si `v_res.id_pedido IS NOT NULL`; `fn_generar_cobro` retorna cobro vigente si ya existe `GENERADO` para el pedido — REQUIERE APROBACIÓN de usuario (aplica schema en cloud). (2) **Publicar** WF-20 (`BopJBfl3CQ9Ck2YL`) y WF-10 (nuevo draft `387fab29-…` → activeVersion) — REQUIERE APROBACIÓN. (3) **QR imagen real**: WF-80 solo envía image vía `media_url` pública; hoy WF-20 envía TEXTO con monto+referencia. Para enviar la imagen: hacer bucket `qr-pagos` público (UPDATE `storage.buckets`) y subir PNG a `qr-pagos/{id_comercio}/tienda.png` (acción del usuario; no hay tool de upload en MCP). (4) Resto F2: WF-11/12/13/14, WF-30/31. (5) F1 residual: WF-00, U2 (OAuth meta-devtools), hardening F7 WF-80. Sin commit.

- **2026-08-28 (ejecución 1+2: migración 19, publicación WF-20/WF-10, bucket público, imagen QR)** — Usuario aprobó (1) migración 19, (2) publicar WF-20 + WF-10 v2 y (3) bucket `qr-pagos` público + enviar QR por URL. `rsuelvo-db` aplicó `19_idempotencia_pedido_cobro` en cloud (`iwfaktlxebxtocmswdvv`): `fn_crear_pedido_desde_reserva` retorna el pedido existente si `v_res.id_pedido IS NOT NULL`; `fn_generar_cobro` retorna el cobro `GENERADO` vigente del pedido (cierra H-1). Verificado `prosecdef=true` + `search_path=rsuelvo,public`. `UPDATE storage.buckets SET public=true WHERE name='qr-pagos'` aplicado y verificado (bucket ya sirve la imagen). Vault sincronizado: nuevo `19_idempotencia_pedido_cobro.sql` + `06_functions.sql` + `_monolito`. `rsuelvo-n8n` modificó WF-20 (`BopJBfl3CQ9Ck2YL`): "Preparar WF-80 Input" ahora emite `type:'image'` + `media_url=${SUPABASE_PROJECT_URL}/storage/v1/object/public/qr-pagos/{id_comercio}/tienda.png` + `text` caption (monto/referencia) — envía la IMAGEN del QR pública vía WF-80. WF-10 (`TSC1otHnDCr0ADiW`): añadido IF `¿Reserva ya existente?` (`resultado=='RESERVA_YA_EXISTENTE'`) → mismo `Call WF-20` (H-2); `SIN_STOCK` NO conectado a WF-20. **Publicados**: WF-20 `2947625b-8926-4865-9f59-acd163406842`, WF-10 `9f04d398-b6d7-4b5c-9392-6ec8c19cab39` (draft con H-3+H-2). Matriz §6: H-1 y H-2 marcados ✅ CERRADO. **Pendiente del usuario**: subir el PNG del QR a `qr-pagos/{id_comercio}/tienda.png` (ruta para FER = `qr-pagos/3cf9a53a-cf8c-4482-b752-ab64c2136875/tienda.png`) para que el envío de imagen funcione; hasta entonces el envío de imagen dará 404 en Meta. Sin commit.

- **2026-08-28 (debug: QR image 400 + bucle de status webhook)** — Tras subir el PNG y reenviar `FERA01`, el buyer (59172066766 / Ivan) recibió el TEXTO ("Producto reservado…") pero NO el QR. Traza n8n: WF-02 (187 success)→WF-10 (190)→WF-20 (191)→WF-80 (192 imagen, 193 texto) todos success y Meta devolvió `wamid` (sent). Pero el webhook de STATUS de Meta (exec 194) reportó `status:"failed"`, `code:131053 "Media upload error: Downloading media from weblink failed with http code 400"`. **Causa raíz:** WF-20 armaba `media_url = qr-pagos/{id_comercio}/tienda.png` pero el usuario subió `tienda.png` a la **RAÍZ** del bucket (verificado en `storage.objects`: `name='tienda.png'`, `path_tokens=['tienda.png']`; la carpeta `3cf9a53a-…/` solo tenía `.emptyFolderPlaceholder`). La URL con carpeta daba 404→Meta 400. `curl` confirmó raíz `qr-pagos/tienda.png` → HTTP 200 (image/png 108KB). **Fix WF-20** (`rsuelvo-n8n`): `media_url` ahora apunta a la raíz `qr-pagos/tienda.png`. **Segundo bug:** WF-02 moría con `23502 null value in column external_message_id` al registrar webhooks de STATUS (Meta reintentaba en bucle, 4 execs error). **Fix WF-02:** "Normalize Meta Payload" marca `type='status'` cuando hay `value.statuses`; IF `¿Es status webhook?` → `Status ACK` (terminal, 200) sin llamar `fn_registrar_evento_whatsapp`; rama FALSE intacta. **Publicados**: WF-20 `8cf9a9fc-42c4-4931-b39d-ef2abb516b54`, WF-02 `79a89373-2436-4da9-afe3-25eea31a2683`. **Caveat:** el QR ahora es compartido a nivel bucket (no por tenant); al habilitar multitenancy subir a `qr-pagos/{id_comercio}/tienda.png` y revertir WF-20. **Pendiente**: reenviar `FERA01` para validar que el QR imagen llega. Sin commit.

- **2026-08-28 (F2 — WF-12 Lista de espera construido + cableado WF-10 SIN_STOCK)** — `rsuelvo-n8n` (hy3-free) creó `RSU|12|Lista de Espera` (sub-workflow, ID `cbFrDQ4awnTHRl48`): trigger `When Executed by Another Workflow` (input id_comercio/id_sucursal/id_variante/id_cliente/phone/provider) → `Check Lista Espera` (Postgres, pre-check activos/maximo/pos_existente) → `Ya en lista?` / `Capacidad llena?` (IF) → `Agregar Lista Espera` = `SELECT rsuelvo.fn_agregar_lista_espera($1,$2,$3,$4)` (onError→`Obtener Posicion Existente`, idempotente) → `Obtener Posicion` → `Msg Agregado`/`Msg Ya en lista`/`Msg Lista llena`/`Msg Ya en lista (error)` → `Output` = `message_payload{type,phone,provider,text}` con `#posicion` (español, mínimo). **Bug hallado y corregido por el orquestador:** la query de `Check Lista Espera` venía sin la palabra `SELECT` inicial (sintaxis inválida) → fix aplicado y **WF-12 republicado** `84fff237-c96d-45ca-bf2c-347d3ff383f7`. WF-10 (`TSC1otHnDCr0ADiW`) actualizado (rsuelvo-n8n): removida conexión `PG fn_solicitar_reserva`→`Build Output`; añadidos IF `¿Sin stock?` (resultado=='SIN_STOCK') y nodo `Call WF-12 (Lista Espera)`; `¿Reserva ya existente?`→(true→WF-20 y →`¿Sin stock?`) y (false→`¿Sin stock?`); `¿Sin stock?` true→WF-12→`End`, false→`Build Output`. **Publicado** `16e2531a-c31e-4d59-901d-5c3af9e089d9`. WF-12 retorna `message_payload` y WF-10 lo reenvía a WF-02→WF-80 (D11; WF-12 NO llama WF-80). Verificación: `fn_agregar_lista_espera(uuid,uuid,uuid,uuid)` ejecuta (prueba ROLLBACK con cliente dummy falló solo por FK; lógica OK) y `fn_tiene_acceso_sucursal` arranca con `fn_es_service_role()=true` para el rol postgres de n8n. Matriz: fila 12 → build publicado `cbFrDQ4awnTHRl48` v`84fff237`; fila 10 → rama SIN_STOCK→WF-12. **Pendiente**: test E2E SIN_STOCK real (requiere stock=0 temporal + envío del usuario; ofrecido). Siguiente en orden del usuario: WF-13. Sin commit.

- **2026-08-28 (F2 — WF-13 Notificar lista de espera construido)** — `rsuelvo-n8n` (hy3-free) creó `RSU|13|Notificar Lista Espera` (ID `VADWZuYe7MsPXIBd`, **publicado** `7f530a76-ecf1-41b7-8d42-6a176c1837de`): Schedule cada 1 min → `Get Waiting Groups` (Postgres: `SELECT DISTINCT id_comercio, id_sucursal, id_variante FROM rsuelvo.tbl_lista_espera WHERE estado='ESPERANDO'`) → `Notify Next in Queue` = `SELECT rsuelvo.fn_notificar_siguiente_lista_espera($1,$2)` (una vez por grupo; simplificación MVP: un notify por corrida por grupo) → IF `resultado=='CLIENTE_NOTIFICADO'` → `Lookup Client & Product` (join cliente/variante/config/canal) → `Build Message Payload` (texto §28: SKU/Producto/Precio/tiempo_aceptacion_lista_espera_minutos/Responde SI-NO) → `Send via WF-80` (`ISxev9AssaAvQa8z`, provider en minúsculas) con `onError:continueErrorOutput`. Opt-out resuelto dentro de WF-80 (`fn_cliente_optado`). Matriz fila 13 → build publicado `VADWZuYe7MsPXIBd` v`7f530a76`. Regla 3 (n8n orquesta, BD decide) y D11 (salida única WF-80) cumplidas. Sin commit.

- **2026-08-28 (F2 — WF-14 Aceptar oportunidad construido)** — `rsuelvo-n8n` (hy3-free) creó `RSU|14|Aceptar Oportunidad` (ID `WjaBON6zZpA6nexq`, **publicado** `ac3eadac-4b0f-471f-9a8d-cc322304f9c7`), sub-workflow disparado por WF-80 (inbound SI): input {id_comercio, phone, provider} → `Resolve Provider` (lookup `tbl_canal_whatsapp` si provider vacío) → `Resolve Opportunity` (la `NOTIFICADO` vigente del cliente) → IF existe → `Accept Lista Espera` = `SELECT rsuelvo.fn_aceptar_lista_espera($1::uuid)` (reserva atómica vía `fn_solicitar_reserva`) → IF `RESERVA_CREADA` → msg "¡Reserva confirmada!…" sino "oportunidad expiró…" → `Build Message Payload` → `Send via WF-80` (`ISxev9AssaAvQa8z`). Alcance: cubre SOLO respuesta SI; NO/declinar queda como follow-up. Matriz fila 14 → build publicado `WjaBON6zZpA6nexq` v`ac3eadac`. Sin commit.

- **2026-08-28 (F2 — WF-30 Expirar reservas + cierre WF-31)** — Verificado en cloud: **pg_cron NO está activo** (`cron.job` ausente, extensión `pg_cron` no habilitada) aunque `fn_procesar_reservas_vencidas`/`fn_expirar_reserva` existen. Por eso se construyó `RSU|30|Expirar Reservas` (ID `Sy5hoHc0HzFEgzcl`, **publicado** `d3b5ebd3-a644-4f35-82a3-d6d7e5174de5`) como la "Alternativa n8n" documentada en `12_cron.sql`: Schedule 1 min → `SELECT rsuelvo.fn_procesar_reservas_vencidas(200)` (onError:continueErrorOutput), sin salida WhatsApp. **WF-31** NO se construye por separado: el notify-next lo cubre WF-13 (pollea `ESPERANDO` + `fn_notificar_siguiente_lista_espera` + WF-80). Matriz fila 30 → build publicado `Sy5hoHc0HzFEgzcl` v`d3b5ebd3`; fila 31 → estado "✅ cubierto por WF-13 + WF-30". **F2 COMPLETADA**: cadena compra WhatsApp + lista de espera + expiración (WF-10→20→80, WF-12→13→80, WF-14, WF-30) construida y publicada. Resta test E2E SIN_STOCK real (ofrecido al usuario: setear stock=0 + enviar SKU). Sin commit.

- **2026-08-28 (F1 — F7 hardening WF-80 publicado)** — `rsuelvo-n8n` (hy3-free) implementó el hardening F7 del gateway `RSU|80|WhatsApp Gateway` (ID `ISxev9AssaAvQa8z`) en **draft v6** (`6f0949bc-6766-4644-82eb-3117636d57ca`) y el orquestador **autorizó publish** (no destructivo; la sesión ya publicó WF-12/13/14/30 sin incidentes). Cambios: **circuit breaker por provider** en `$workflowStaticData` (`cb_<provider>`: 5 fallos → abierto 60s; 401/403 abre directo; cierra tras 2xx y loguea `CIRCUIT_OPEN`/`CIRCUIT_CLOSED` en `tbl_logs_auditoria`); **rate-limiter por comercio** (`rl_<id_comercio>`: 20 envíos/60s, devuelve `rate_limited` + log `RATE_LIMIT`); **routing Meta/OpenWA por ítem** (corrige bug de `.first()` de la versión previa); `retryOnFail` (maxTries 4, backoff 1/2/4s) en los nodos HTTP Meta/OpenWA. Validación pinData (exec 411 meta, 413 openwa): `sendStatus:'sent'` ✓. **Publicado** `6f0949bc-6766-4644-82eb-3117636d57ca`. Matriz fila 80 → ✅ PUBLICADO. **Follow-up P2**: cola física (tabla+worker) y rate-limit por comercio leído desde `tbl_comercio_config` (hoy constante 20/60s hardcodeada en static data, tolerable MVP pero viola Regla de Oro 10 → P2). F1 ahora solo resta U2 (OAuth meta-devtools, bloqueado por usuario). Sin commit.

- **2026-08-28 (F3 — encadenamiento WF-21→22→23→24 publicado)** — `rsuelvo-n8n` (hy3-free) verificó y completó el cableado del flujo de verificación de comprobante (F3). WF-21 (`7cdVw8JhbW5JJJtf`) **rewrite sin OCR inline** → `Pedido encontrado?` → `OCR via WF-22` (`spc0XkjO9b3REguW`, **publicado** `cdd81989-6307-41cc-9d6f-9a10f9eccbaf`, GPT-4o Vision **solo extrae JSON**, no decide) → `Registrar Comprobante (fn_registrar_comprobante)` + `Iniciar Verificación (fn_iniciar_verificacion)` → `Verificación estado?`: `VERIFICACION_INICIADA`→WF-80 ack + `Call WF-23` (`Cg7r8oNlCVDSdoXb`, **publicado** `d617f180-3b75-443a-8d26-6702f69b61e4`, decide CONFIRMAR/RECHAZAR/MANUAL vía `fn_confirmar_pago`/`fn_rechazar_verificacion`) → WF-24 (`xvbVZJl8gdolFY54`, **publicado** `230e5bc6-0201-41b5-a520-b41ccea89fe8`, pide datos envío al cliente vía WF-80). Ramas `SIN_CREDITOS`/`ESTADO_NO_VERIFICABLE`/`Comprobante ilegible`/`NoOrder` → WF-80. **Publicado WF-21** `edac1e0e-7fec-48b2-a842-0e5110e6381d`. Firmas SQL verificadas por el orquestador en `06_functions.sql`: `fn_iniciar_verificacion(p_id_comprobante,p_codigo_servicio,t_forzar)` retorna `VERIFICACION_INICIADA`/`SIN_CREDITOS`/`ESTADO_NO_VERIFICABLE`; `fn_confirmar_pago(p_id_verificacion,p_resultado jsonb)` y `fn_rechazar_verificacion(p_id_verificacion,p_resultado jsonb)`. Cumple Reglas de Oro 2/4/8/10 y D11 (salida única WF-80). Matriz filas 21/22/23/24 actualizadas al chaining. **Observación (no bloquea):** WF-23 y WF-24 ambos invocan `fn_confirmar_pago` (doble llamada); la función es idempotente (re-set VALIDO/COMPLETADA), así que no rompe, pero conviene revisar para evitar duplicar transición de pedido→PAGADO en P2. **Resta:** test E2E real envío de comprobante (requiere captura de pago del usuario) y U2 (meta-devtools). Sin commit.

- **2026-08-29 (migración de n8n cloud a cuenta trial nueva)** — El free tier de la cuenta original (`rsuelvo2026.app.n8n.cloud`, ethancardenas69) se agotó; se descartó Railway self-host (complejidad innecesaria para desarrollo) y el usuario creó cuenta trial nueva `rsuelvo.app.n8n.cloud` (**ivanluiscardenas@gmail.com**, 14 días). Export ZIP `rsuelvo2026-workflows.zip` (14 RSU + 3 legacy). Import + republicación en la nueva cuenta: los IDs de workflow se preservaron al importar, PERO n8n exige re-publicar sub-workflows en orden de dependencia (80→22→24→23→21→20→10→03→04→02→12→13→14→30) y re-referenciar manualmente cada nodo `executeWorkflow` con el ID real del WF-80 local. Usuario configuró `$vars` (META_SYSTEM_TOKEN/META_VERIFY_TOKEN/SUPABASE_PROJECT_URL) y credenciales. Webhook producción: `https://rsuelvo.app.n8n.cloud/webhook/webhooks/whatsapp/meta`; faltaba suscribir el campo `messages` en Meta → al suscribirlo, la cadena WF-02→…→WF-21 ejecuta. **Nota MCP:** el MCP n8n de opencode quedó apuntando a la cuenta vieja (tokens con mismo `sub`); conexión a la nueva cuenta no lograda (auth UI falla) → trabajo n8n vía UI manual del usuario. Sin commit hasta este hito.

- **2026-08-29 (migraciones 20/21 — idempotencia de reenvío de comprobantes)** — Test E2E del comprobante falló con `duplicate key violates unique constraint uq_comprobante_operacion_comercio` (índice único anti-fraude `(id_comercio, numero_operacion) WHERE numero_operacion IS NOT NULL`): el comprobante `TXN123456` quedó registrado `RECIBIDO` el 08-28 23:08 con verificación nunca iniciada (muerte de n8n cloud), y el reenvío de la MISMA imagen chocaba el índice. **Diagnóstico clave: el OCR (WF-22/GPT-4o) SÍ funciona** — extrajo `TXN123456` de nuevo; el fallo era downstream. Fix aplicado a cloud (`iwfaktlxebxtocmswdvv`): **migración 20** `comprobante_reenvio_idempotente` — `fn_registrar_comprobante` misma firma; si existe (id_comercio, numero_operacion): mismo pedido → UPDATE datos y reutiliza comprobante (flujo continúa a verificación, cumple D5 reenvío); otro pedido → excepción `COMPROBANTE_DUPLICADO` (anti-fraude se mantiene). **Migración 21** `fix_ambiguedad_comprobante` — corrige error PL/pgSQL `column reference "id_comprobante" is ambiguous` (en RETURNS TABLE esos nombres son variables de salida; el WHERE del UPDATE ahora califica con alias `c`). Vault sincronizado: nuevos `02-Base-de-Datos/sql/20_…` y `21_…` + `06_functions.sql` actualizado. WF-21 sin cambios (contrato intacto). **Incidencia I-02 abierta:** el monolito `_monolito_…_v2.sql` tiene gap preexistente (nunca incluyó `fn_registrar_comprobante` de migración 14) — pendiente regenerar. **Próximo paso acordado:** auditoría integral (WFs n8n ↔ vault ↔ BD ↔ flujo) antes de continuar pruebas E2E del comprobante. Commit de este hito.

- **2026-08-31 (F3 COMPLETADA — E2E de pagos validado de punta a punta)** — Tras migración 22 (auditoría Claude) y fixes sucesivos en n8n (nueva cuenta `rsuelvo.app.n8n.cloud`): **cadena completa SKU→reserva→pedido→QR→comprobante→OCR real→verificación→confirmación→PAGADO→WhatsApp de datos de entrega ejecutada con éxito**. Fixes aplicados durante la depuración: **migración 23** `updated_at_verificaciones_metodos_pago` (Aud-H-11: triggers `trg_*_updated_at` sobre tablas sin la columna — dormía desde F0, explotó en el primer INSERT real de verificación); **migración 24** `comprobante_prefiere_reserva_activa` (el resolver de `fn_registrar_comprobante` prefiere pedidos con reserva ACTIVA antes del fallback al último ESPERANDO_PAGO — evita ligar comprobantes a reservas muertas); **migración 25** `confirmar_pago_guarda_reserva_viva` (guardas H-12: pedido ya PAGADO→`YA_PROCESADO`; reserva no ACTIVA o vencida→`RESERVA_VENCIDA` en vez de excepción de inventario; pool de stock re-sincronizado tras desincronización detectada). **WF-22 extractor real**: el mock ("Mock OCR Result" hardcodeado TXN123456/Juan Pérez — hallazgo del usuario) fue reemplazado por **Google Gemini vía HTTP Request** (`gemini-3.6-flash:generateContent`, key en `$vars.GEMINI_API_KEY`, binary→base64 vía nodo "Extract from File" por el modo `filesystem-v2`, `thinkingBudget:0` + `maxOutputTokens:800`); cumple Regla 4 (extrae, no decide) — actualizar refs "GPT-4o Vision" del PROMPT/Matriz en próxima pasada. Fixes n8n menores: `jsonHeaders` dict→string (WF-23 Rechazar / WF-24 Confirmar Pago), `provider` mayúsculas→`.toLowerCase()` en builders WF-23/24 + Switch de WF-80 blindado, campo `provider` con `=` literal residual, reconstrucción de "Build WF-80 Confirm" (5 assignments). Re-test E2E final: comprobante real (`0726...4804`? — operación extraída por Gemini) → `VALIDO`→`COMPLETADA` (−1 crédito)→pedido **PAGADO**→reserva **CONFIRMADA**→stock −1→movimiento VENTA→**WhatsApp "datos de entrega" recibido por el comprador** ✓. **Deuda hacia F5:** la RESPUESTA del comprador con datos de entrega aún no tiene flujo que la capture (WF-40/41/42 + `fn_crear_envío` — formato propuesto `NOMBRE:/DIRECCIÓN:/REFERENCIA:/TELÉFONO:` por líneas). Commit de cierre F3.

- **2026-08-29 (auditoría externa Claude + migración 22)** — Auditoría externa ejecutada por Claude con conectores Supabase MCP + n8n MCP (Capa C/vault NO verificada: repo privado sin conector GitHub; reporte en `~/Escritorio/Auditoria-RSUELVO-2026-08-29.md`). Hallazgos: **Aud-H-01 BLOQUEANTE** — `fn_confirmar_pago` sin guarda de idempotencia (re-invocación duplicaba stock + movimiento VENTA, Regla 2). **Aud-H-02 BLOQUEANTE** — WF-02 en cuenta nueva (`kWGXKtmjdsL2Nrkc`): nodo `Route to WF-03` con salidas éxito/error fusionadas en main[0] → `Close Event Error` corre también en camino feliz (Regla 7). **Aud-H-03 ALTO** — pg_cron SÍ está activo (job 1, `* * * * *`, `fn_procesar_reservas_vencidas(200)`): contradice bitácora previa; expiración corre desde BD. **Aud-H-04** — Schedule Trigger de WF-30 mal configurado (`field:hours` → corre cada 1h, no 1min). **Aud-H-05** — rate-limit WF-80 hardcodeado (Regla 10). **Aud-H-06** — SQL por concatenación en 5 nodos Postgres de WF-80 (contexto: binding `queryReplacement` falló en smoke 6; interpolación fue fix deliberado). **Aud-H-07** — `audit_insert` de `tbl_logs_auditoria` demasiado permisivo. **Aud-H-08** — IDs de WFs obsoletos en docs tras re-import (IDs reales nuevos: WF-02=`kWGXKtmjdsL2Nrkc`, WF-80=`CDizafXZQCL5SjOz`, etc. — tabla completa en Matriz §0). **Aud-H-09** — migraciones 20/21 verificadas correctas. **Aud-H-10** — umbrales OCR 0.8/0.5 hardcodeados en WF-23. **Resueltos por el orquestador (migración 22 `fix_idempotencia_confirmar_pago_y_rate_limit_config`, aplicada y verificada):** Aud-H-01 (guarda `YA_PROCESADO` si verificación COMPLETADA), Aud-H-05-BD (columna `tbl_comercio_config.rate_limit_whatsapp_por_minuto DEFAULT 20`), Aud-H-07 (`audit_insert` → solo `fn_es_superadmin()`; verificado que roles n8n son BYPASSRLS `postgres`/`service_role`, sin impacto). **Incidencia I-02 CERRADA:** monolito ahora incluye `fn_registrar_comprobante` (versión 20/21). Vault sincronizado: `22_…sql` nuevo + `03_tables.sql` (columna) + `06_functions.sql` (guarda) + `08_rls.sql` (política) + monolito (3 cambios) + Matriz (§0 IDs reales, §6 hallazgos auditoría) + `workflows.md` (banner cuenta activa). **Pendiente usuario (fix manual n8n, cuenta `rsuelvo.app.n8n.cloud`):** Aud-H-02 (recablear `Route to WF-03`: éxito→`Prepare WF-80 Input`, error→`Close Event Error`, 2 salidas visibles), decisión Aud-H-03 (recomendado: mantener pg_cron y DESACTIVAR WF-30), P2: Aud-H-05-WF (leer `rate_limit_whatsapp_por_minuto`), Aud-H-06 (query params), Aud-H-10 (umbrales config). Pendientes auditoría: WFs 03/04/10/12/13/14/20/21/22 a fondo, D11 sweep, advisors, Capa C completa.

- **2026-09-02 (E2E lista de espera + pago validado EN SOLITARIO — F2/F3 probadas de punta a punta)** — Prueba completa con un solo WhatsApp (cliente sintético A creado por SQL como tenedor de stock): **SKU → reserva → QR → (vencimiento) → lista de espera → cron event-driven → webhook → notificación → SI → aceptación → QR → comprobante → OCR Gemini → PAGO_CONFIRMADO → PAGADO + CONFIRMADA + stock −1 + VENTA** — todo verificado en BD. Fixes de flujos aplicados durante la depuración: **WF-12** ("Output" Set→Code node sin pairing; "Agregar Lista Espera" interpolación directa); **WF-13** (Schedule→Webhook event-driven; queries a Expression mode con interpolación; Lookup con `COALESCE(c.telefono_whatsapp, c.telefono)` — D10: el teléfono vive en whatsapp); **WF-04** (`is_acceptance` en Enrich & Classify + IF de aceptación en la rama text + Dispatch WF-14 con mapping RESTAURADO — se vaciaba al editar); **WF-14** (autónomo: Resolve Opportunity por teléfono devuelve id_lista_espera/id_comercio/id_sucursal/id_cliente/sku; Resolve Provider sin id_comercio; Build desde Resolve Opportunity — cero dependencia del pairing); **WF-21** (jsonBody a Expression mode sin `=` internos; **`p_id_pedido` ELIMINADO del registrar** → migración 24 aplica; order asc→desc); **WF-24** (hardening: IF `resultado=PAGO_CONFIRMADO` + rama FALSE "Tu reserva expiró…" con datos completos). **Protección anti-duplicado PROBADA en producción:** el webhook del comprobante ejecutó 2 veces; la 1ª confirmó (PAGADO+VENTA), la 2ª fue bloqueada por las guardas H-12 (migración 25) con mensaje amable en vez de doble venta o WF-80 roto ✓. **Hallazgo Aud-H-15:** el nodo "Resolve Pending Pedido" de WF-21 usaba `order=created_at.asc` (el pedido MÁS VIEJO) y pasaba `p_id_pedido` explícito → bypass total de la migración 24 → fix: p_id_pedido eliminado + order desc. **Incidencia nueva:** existen 2 copias de WF-02 (la activa es el duplicado `NpZymtlm0CAaHWfW`; original `Kem88sByaFk6bUNO` inactivo) — limpieza pendiente + auditar referencias. **P2:** rama NO de lista de espera; dedup de webhooks duplicados; distinguir YA_PROCESADO vs RESERVA_VENCIDA en el texto de WF-24; restaurar `tiempo_reserva_minutos` ya en 10 ✓. Commit de cierre.

- **2026-09-02 (migraciones 27/28 — D14 créditos por venta, paso 1 del plan V1)** — Aplicadas y verificadas en cloud (`iwfaktlxebxtocmswdvv`): **migración 27** — enum `CONSUMO_VENTA` (agregado; `CONSUMO_VERIFICACION` se conserva para V2), `tbl_movimientos_creditos.usuario_id` (atribución del cajero vía JWT/fn_current_usuario_id, NULL en n8n), servicio `VERIFICACION_MANUAL` (costo 0), `fn_consumir_credito_venta(p_id_comercio, p_id_pedido)` (SD-1: permite saldo negativo, referencia PEDIDO), y `fn_confirmar_pago` consume 1 crédito por venta en la misma transacción (tras el insert de VENTA, antes del RETURN; SD-5 rechazos no consumen; SD-6 aplica a toda venta). **Migración 28** — eliminados los checks de no-negatividad (`saldo_actual/saldo_anterior/saldo_posterior >= 0`) que impedían SD-1; se conserva `cantidad <> 0`. Fix incluido: CHECK de `costo_creditos` relajado a `>= 0` (servicio gratuito). **SD-7 abierta:** D14 aprobado como 1 crédito por confirmación; si pricing futuro requiere por-unidad, cambiar literal 1 por v_res.cantidad en fn_confirmar_pago. Vault sincronizado (02_enums, 03_tables, 10_seed, 06_functions, monolito, archivos 27/28). Pendiente siguiente paso del plan V1: branch `verificacion_automatica` en WF-21 (instrucciones al usuario) y wireframes app cajero (F4). Commit incluido.

- **2026-09-04 (FASE A — modo manual D13 validado E2E completo + D14 en vivo)** — Prueba real con un solo WhatsApp: **SKU → reserva → QR (mensaje único) → comprobante → [Gemini 429 → rama "¿Error técnico del OCR?" derivó a RECIBIDO sin OCR ✓ P2 validado en producción] → verificación VERIFICACION_MANUAL (PENDIENTE, creditos_consumidos=0 ✓ D-E) → ack "Lo estamos verificando…" → [simulación del botón del cajero por SQL] → PAGO_CONFIRMADO → pedido PAGADO + reserva CONFIRMADA + stock −1 + VENTA + CONSUMO_VENTA (86→85, D14 en vivo ✓)**. Fixes n8n aplicados por Claude (MCP): WF-21 (OnError en OCR via WF-22 + rama duplicado FALSE → Msg Error Genérico → Send; jsonBody verificado sin = internos), WF-02 copia activa (H-02 corregido: Close Event Error movido a salida error + nombres truncados reparados + referencia $('Route to WF-03') actualizada). **Hallazgo Aud-H-15 confirmado en vivo:** el nodo "Resolve Pending Pedido" de WF-21 (order=created_at.asc) ligaba el comprobante al pedido más VIEJO → fix aplicado (p_id_pedido eliminado del registrar → migración 24 resuelve; order desc). **Bug migración 27 hallado y corregido (migración 29):** servicios de costo 0 (VERIFICACION_MANUAL) generaban movimiento con cantidad=0 → violaba cantidad<>0 → fix: costo 0 = sin movimiento (trazabilidad via creditos_consumidos=0 en la verificación). **También corregida migración 28 (checks de saldo negativo eliminados)** — sin ellas SD-1 era imposible. **F5 n8n pendiente:** los 3 workflows de entrega (Request Datos / Registrar Datos / Notificar Estado) + regla WF-04 se construirán con hermes agent (prompt completo entregado) — la capa BD ya está lista (migración 30: fn_registrar_entrega + triggers pg_net pedido PAGADO / envío estado). **El trigger pedido→PAGADO ya disparó** el webhook /entrega/request (404 inofensivo hasta que WF-25 exista). Commit de cierre.

- **2026-09-04 (F5 n8n construido por hermes agent — 3 workflows de entrega publicados)** — hermes agent (MCP n8n) aplicó el prompt del orquestador: **WF-25-A** `8lXZxNcywbuNaG2p` "RSU | 25 | Entrega | Request Datos" (9 nodos: Webhook entrega/request → token → Lookup Pedido → PAGADO? → Build Msg → Send via WF-80 → Responds) · **WF-25-B** `ESVUUxhjEdpK9SkX` "RSU | 25 | Entrega | Registrar Datos" (13 nodos: dispatch desde WF-04 rama texto → Parse Datos tolerante → fn_upsert_cliente_json (existe en BD ✓) → Resolve Pedido PAGADO más reciente → fn_registrar_entrega → Msg Preparacion → WF-80; rama incompleto → Msg Datos Incompletos) · **WF-25-C** `pwXuXy3AW3fCM5fG` "RSU | 25 | Entrega | Notificar Estado" (11 nodos: Webhook entrega/estado → token → Switch 5 estados → Send via WF-80). **WF-02** copia activa: H-02 corregido (Close Event Error1 → salida error del Route ✓) + nodos renombrados. **WF-21**: verificado jsonBody sin = internos + OCR onError + rama duplicado FALSE → Msg Error Genérico. **WF-30**: desactivado verificado ✓. **Tests hermes:** T1 (exec 1198 ✓ — solicitud de datos entregada al comprador con bypass temporal de token) · T3 (exec 1202 ✓ — notificación EN_RUTA entregada) · T2 pendiente del dueño (respuesta WhatsApp con el formato). **Hallazgos hermes:** PostgREST dispersa arrays 1-fila en items (fallback [0] como seguro) · setNodeParameter con índices de array falla (usar updateNodeParameters + replace) · create_workflow_from_code no asigna credenciales HTTP (setNodeCredential requerido) · mensajes sin tildes/emojis en Code nodes (encoding del parser). **BLOQUEANTE para producción:** la variable ENTREGA_TOKEN (RSU_entrega_notif_7Qk2mXwP) debe crearse MANUAL en n8n UI (no hay tool MCP para variables) — sin ella los triggers pg_net reales caen en 403. **El trigger pedido→PAGADO ya disparó correctamente** (pg_net 404 a las 14:13 — WF-25-A no existía; ahora sí). P2: fn_agregar_lista_espera sin dedup de cliente+variante (entrada 3faf3724 duplicada — eliminada); nombres truncados en nodos del WF-02 duplicado. Commit pendiente con verificación T2 en BD.

- **2026-09-04 (T2/T3 — F5 validado E2E completo: datos → envío → reparto → entrega)** — **T2 ✓**: el comprador respondió el formato NOMBRE:/DIRECCIÓN:/REFERENCIA:/TELÉFONO: → WF-04 (regla DIRECCI) → WF-25-B → fn_registrar_entrega → **tbl_envios PENDIENTE creado** (dirección/referencia/teléfono ✓) + **pedido 1a918c28 → PREPARANDO** ✓ + tbl_clientes.nombre actualizado ✓. Fix intermedio: el IF "Datos completos?" exigía boolean estricto y el `&&` devolvía string ("NICO TEST") → fix `!!(...)` coerción (patrón probado). **T3 ✓**: simulación del repartidor por SQL (fn_actualizar_estado_envio): PREPARANDO → ASIGNADO → EN_RUTA → ENTREGADO (4 transiciones ✓ — la máquina de estados exige PENDIENTE→PREPARANDO primero, hallado en el test) → los triggers pg_net dispararon WF-25-C en cada transición → **el comprador recibió las notificaciones por WhatsApp** (asignado/en camino/entregado — pendiente confirmación del dueño). **F5 (n8n + BD) 100% funcional antes de Flutter.** P2 acumulados: rama NO de lista de espera; dedup de webhooks; texto YA_PROCESADO vs VENCIDA en WF-24; limpieza WF-02 duplicado + eventos PROCESANDO viejos; emojis/tildes en los Code nodes de hermes. Commit de cierre de sesión.

- **2026-09-04/05 (F5 n8n 100% — hermes construyó + Claude auditó/corrigió — T1/T2/T3 ✓)** — hermes agent (MCP n8n) construyó y publicó los 3 workflows de entrega: **WF-25-A** `8lXZxNcywbuNaG2p` "Request Datos" (9 nodos) · **WF-25-B** `ESVUUxhjEdpK9SkX` "Registrar Datos" (13 nodos) · **WF-25-C** `pwXuXy3AW3fCM5fG` "Notificar Estado" (11 nodos) + la regla de datos en WF-04. Claude auditó contra el contrato del orquestador y aplicó 13 fixes: **trampa (b) sistemática de hermes** (workflowInputs.value vacío en 6 nodos → nulls), fn_upsert_cliente_json inexistente → fn_upsert_cliente firma completa, responseFormat autodetect→text (RPCs escalares), parser tolerante a acentos, Capture Pedido Id array-form, !! coerción en IFs, textos con tildes/emojis, Build Asignado copy-paste. **Tests en vivo:** T1 (exec 1275/1276 ✓) solicitud de datos entregada al comprador (pedido 1a918c28) · **T2 ✓ (exec 1288/1289, disparada por el WhatsApp REAL del dueño)** → fn_registrar_entrega → **envío adae108f PENDIENTE + pedido cf54b99c PREPARANDO + tbl_clientes.nombre actualizado** ✓ (verificado en BD por el orquestador) · **T3 ✓ (exec 1294 EN_RUTA, 1298 ENTREGADO)** notificaciones entregadas ✓ (disparadas por el orquestador simulando al repartidor). **Nuevas trampas documentadas:** (g) RPCs que retornan escalar uuid vía PostgREST llegan text/plain → responseFormat: text + limpieza en Code; (h) el && de JS en IFs estrictos devuelve el último operando (string) → usar !! coerción. **Nota:** pedido 1a918c28 (T1) con solicitud enviada sin respuesta del dueño — completar o dejar como artefacto. **F5 (n8n + BD) 100% funcional pre-Flutter.** Commit de cierre de sesión.

- **2026-09-06 (T3 — F5 validado E2E completo: envío → reparto → entrega con notificaciones)** — Simulación del repartidor por SQL (fn_actualizar_estado_envio): **PENDIENTE → PREPARANDO → ASIGNADO → EN_RUTA → ENTREGADO** (4 transiciones ✓ — la máquina de estados exigió PENDIENTE→PREPARANDO primero, hallado en el test). **Los triggers pg_net dispararon WF-25-C en cada transición → el comprador recibió las notificaciones por WhatsApp** (asignado/en camino/entregado — el dueño confirma recepción). **F5 (n8n + BD) 100% funcional pre-Flutter.** El ciclo completo de RSUELVO V1 validado E2E en producción: SKU → reserva → QR → comprobante → verificación híbrida → PAGADO → solicitud de datos (event-driven) → captura → envío → reparto → entrega, todo notificado por WhatsApp y sin un solo polling. Commit de cierre de sesión.

- **2026-09-07 (OBS-001 Momento 2 implementado en n8n por Claude — rechazo de turno en lista de espera)** — Reporte de Claude (MCP n8n): **WF-13** (`SHE66obsbreFcTiT`) ya ofrecía SI/NO en el mensaje desde una sesión previa — sin cambios, no republicado. **WF-04** (`qLyBczowLOcnNXe5`, publicado ✓): nodo "If" de dispatch a WF-14 pasó de combinator `and`/`is_acceptance==true` a combinator `or` con condición añadida `text=='NO'`; el nodo "Dispatch → WF-14" ahora incluye el campo `text` (`$('Enrich & Classify').item.json.text`) — sin él WF-14 no sabría qué respondió el comprador. **WF-14** (`MLgnwfXbg7HnWVHC`, publicado ✓): "Start" acepta `text`; "Resolve Opportunity" ampliado (`id_lista_espera`, `id_comercio`, `id_sucursal`, `id_variante`, `fecha_expiracion`, `id_cliente`, `phone` — mismo WHERE con COALESCE, ventana de expiración intacta); nodos nuevos "Parse Decision" (Code: normaliza mayúsculas/sin tildes → ACEPTAR si SI/OK/DALE, RECHAZAR cualquier otra cosa incl. NO) + IF "¿ACEPTAR?" (TRUE→"Accept Lista Espera" sin cambios; FALSE→"Rechazar Turno" HTTP→`fn_rechazar_lista_espera` → "Notificar Siguiente" HTTP→`fn_notificar_siguiente_lista_espera` → "Msg Turno Liberado" → "Send via WF-80"); reenrutado IF de oportunidad existente TRUE→"Parse Decision". Hallazgos: el bug real era que WF-04 nunca dispatchaba "NO" (caía al fallback) y WF-14 no recibía ni evaluaba `text` (siempre aceptaba) — ambos cerrados. Trampa (a) evitada en los 2 jsonBody nuevos (valores sin `=` interno, expresión completa `={{ {...} }}`); credencial `supabaseApi` (`SLiMpIbRFrcmxGur`) fijada explícitamente con setNodeCredential (n8n no la auto-asigna en HTTP nuevos). Sin tocar BD (`fn_rechazar_lista_espera`/`fn_notificar_siguiente_lista_espera` m31 usadas tal cual). **Pendientes:** (1) **Momento 1 de OBS-001 no implementado aún** (WF-10 rama `SIN_STOCK` → pregunta SI/NO; WF-12 solo si responde "SI"); (2) **tests T-A/T-B por el dueño** (SKU + respuestas SI/NO desde 2 números); (3) en T-A verificar forma de retorno de `fn_notificar_siguiente_lista_espera` (trampa c — nodo solo dispara la RPC, no consume el retorno) y en BD: entrada 1→RECHAZADO, entrada 2→NOTIFICADO o RESERVA_CREADA. Vault sincronizado: Matriz filas WF-04/WF-14 (Momento 2 + m31) + Observaciones-Usuario (estado + historial). Sin commit.

- **2026-09-07 (OBS-001 T-A/T-B validados E2E + migración 32 turno único — hallazgos H-16/H-17/H-18)** — Test completo con 2 números reales. **T-A (Momento 2)**: stock=0 → 2 compradores entran a lista (WF-10→WF-12, posiciones 1/2) → cron (m26) despertó WF-13 → comprador 1 notificado → respondió **NO** → WF-04 dispatchó (or-combinator) → WF-14 Parse Decision RECHAZAR → `fn_rechazar_lista_espera` (m31) → pos#1 RECHAZADO en 1.2s ✓. **T-B**: reset a ESPERANDO + stock=2 → cron → comprador 2 notificado → **SI** → `fn_aceptar_lista_espera` → RESERVA_CREADA + QR → comprobante real (OCR Gemini extrajo operación/monto/pagador) → VERIFICACION_MANUAL (0 créditos, D-E) → confirmación cajero vía `fn_confirmar_pago` (D13, RPC directo) → **PAGO_CONFIRMADO** → pedido 46 PAGADO + reserva CONFIRMADA + stock −1 + **CONSUMO_VENTA −1 (D14)** ✓. **Hallazgos y fixes:** (1) primera corrida fallida — el dueño envió "Envia FERA01" y el router exige SKU puro (`^[A-Z0-9]{6}$`) → cayó a fallback Placeholder sin envío (gap UX registrado: texto libre no reconocido no recibe ayuda); decisión de diseño pendiente (regex embebido vs estricto). (2) **H-16** — el cron re-notificaba grupos con turno en vuelo (ofertas paralelas). (3) **H-17** — el comprador 2 acumuló 2 ofertas activas (FERD01 vieja + FERA01) → su "NO" rechazó la oferta EQUIVOCADA (WF-14 resolvía por `fecha_expiracion ASC`); diagnosticado por Claude (traza completa 14:48:15Z). (4) **H-18** — turnos NOTIFICADO nunca vencían (zombi FERB01 de 3 días). **Migración 32 `lista_espera_turno_unico` aplicada y validada en vivo**: paso 0 vence NOTIFICADO expirados (3 zombis vencidas por el primer tick 15:04:00), guarda anti-re-notificación de grupo con turno en vuelo, y `fn_notificar_siguiente_lista_espera` salta clientes con oferta activa en cualquier variante (un cliente jamás tiene 2 ofertas — requisito Opción C). Vault sincronizado: nuevo `32_…sql` + `06_functions.sql` + `12_cron.sql` (comentarios) + monolito (cuerpo m32 + gap m26 del cron fn cerrado) + Matriz (§6.b H-16/17/18, filas WF-13/14). **Pendientes:** WF-14 `ORDER BY fecha_notificacion DESC` (hardening H-17, para Claude); decisión UX SKU embebido vs estricto; mensaje de ayuda en el fallback de WF-04 (Placeholder actual no envía nada); eventos stuck en PROCESANDO (higiene, el close-event no corre en todos los caminos); T2 del ciclo de entrega de este pedido (respuesta formato NOMBRE:/DIRECCIÓN:…) o dejar pedido 46 como PAGADO. Sin commit aún.

- **2026-09-07 (E2E completo con FERE01: reserva → lista de espera → aceptación → pago → logística ENTREGADO)** — Producto nuevo creado en cloud: **FERE01** "Producto Test E2E" (150 Bs, stock 2, producto `f09018e8`, variante `5697b0b8`; trigger respetó SKU explícito; `fn_resolver_variante_por_sku` verificado). **Purga de eventos:** 111 eventos stuck en PROCESANDO (desde el 27-08) → marcados ERROR (higiene; el close-event de WF-02 sigue sin correr en todos los caminos — fix n8n pendiente). **Flujo:** comprador 1 envía FERE01 → reserva `8dbf5d8a` ACTIVA + QR ✓ · tenedor sintético (cliente `6de0ea70`, reserva `7a2c0a84` vía `fn_solicitar_reserva` — Opción B/H-2 probada: cliente distinto reserva en paralelo) · comprador 2 envía → SIN_STOCK → lista de espera pos #1 (`90f63a94`) ✓ · reserva del comprador 1 venció (acelerada por test; el tick natural de pg_cron la venció antes de la corrida manual) → cron liberó stock + despertó WF-13 (guardas m32: turno único ✓) → comprador 2 notificado → **SI** → `fn_aceptar_lista_espera` → RESERVA_CREADA + QR ✓ · comprobante real (OCR: operación `14062026/295/398/016/8009`, monto 100 Bs vs 150 esperado — el cajero registra ambos, D13 humano decide) → confirmación cajero vía `fn_confirmar_pago` → **PAGO_CONFIRMADO**: pedido 48 PAGADO + reserva CONFIRMADA + lista → **CONVERTIDO_RESERVA** (estado terminal de éxito del ciclo OBS-001 ✓) + stock 2→1 + CONSUMO_VENTA 82→81 (D14) ✓ · datos de entrega capturados (WF-25-A/B: envío `70923793` PENDIENTE + pedido PREPARANDO) · simulación repartidor: PREPARANDO→ASIGNADO→EN_RUTA→**ENTREGADO** (máquina de estados + notificaciones pg_net) ✓. **Ciclo comercial RSUELVO completo validado de punta a punta con producto nuevo.** Pull del repo: llegaron **OBS-002** (formato mensajes reserva/lista/oportunidad, UX bajo) y **OBS-003** (puntos de entrega por sucursal + transportadoras; elimina dirección libre; 🔴 alto) — a trabajar antes de Momento 1. Sin commit de esta entrada aún.

- **2026-09-07 (OBS-002 implementada por Claude — mensajes reformateados + bugs de corrupción corregidos)** — Claude (MCP n8n) reformateó los 3 mensajes sin tocar BD: **WF-10** v`332bd37d` ("Build Mensaje Reserva Confirmada" en Set: ✅ + variante/SKU/monto + vigencia HH:MM La_Paz + instrucción de comprobante); **WF-12** v`eb06f845` ("📋 Producto ya reservado… Posición: #N" — eliminada la ambigüedad "reservado o sin stock"); **WF-13** v`5632aa56` ("🎯 ¡Ya está disponible!" con sku/variante/precio + minutos desde `rsuelvo.tbl_comercio_config` — el lookup ya existía en "Lookup Client & Product", sin hardcodeo, Regla 10). **Bugs hallados y corregidos de paso:** (1) WF-10 tenía DOS bloques jsCode duplicados y el que ejecutaba NO calculaba hora_expiracion → el `[HH:MM]` salía vacío en producción; corregido con `DateTime.fromISO(...).setZone('America/La_Paz').toFormat('HH:mm')`; (2) misma corrupción de parameters anidados duplicados (texto "bueno" en la parte muerta) reparada en WF-12/WF-13 — patrón de sesión de edición previa, vigilar en futuras ediciones por MCP. Validación: pinData WF-10 (exec 1593, hora "16:30" ✓) y WF-12 (exec 1595, "Posición: #3" ✓, encoding intacto); WF-13 validado por nodo (token check del webhook impide pinData E2E — correcto, no se tocó el secreto). Matriz filas 10/12/13 actualizadas. Validación en producción pendiente (próximo E2E con compras reales). Sin commit aún.

- **2026-09-07 (migración 33 — puntos de entrega + transportadoras, OBS-003)** — Decisiones de diseño cerradas por el dueño: (1) el repartidor SOBREVIVE y lleva productos a los puntos; (2) saltos de estado permitidos; (3) la tienda despacha a la transportadora y registra `numero_guia` al entregar; (4) wireframes graduales. Aplicada a cloud `iwfaktlxebxtocmswdvv`: **`tbl_transportadoras`** (nombre, ciudades, activo) + **`tbl_puntos_entrega`** (id_sucursal FK, tipo CHECK RETIRO_EN_TIENDA/PUNTO_LOCAL/ENVIO_TRANSPORTE, nombre, ciudad, direccion, referencia, id_transportadora FK con CHECK de coherencia, activo, orden) con RLS (patrón `fn_tiene_acceso_sucursal` copiado de tbl_inventario), triggers audit+updated_at, grants (patrón m20); **`tbl_envios.id_punto_entrega`** FK añadida (`numero_guia` ya existía). Funciones: **`fn_listar_puntos_entrega(p_id_sucursal)`** (catálogo activo ordenado con número de opción, SECURITY DEFINER — n8n solo muestra, Regla 3); **`fn_actualizar_estado_envio` v3** (saltos hacia adelante por rank — PENDIENTE<PREPARANDO<ASIGNADO<EN_RUTA<ENTREGADO; NO_ENTREGADO solo desde ASIGNADO/EN_RUTA con observación; CANCELADO desde no-terminales; reintento NO_ENTREGADO→EN_RUTA preservado; seguimiento + trigger pg_net m30 intactos); **`fn_registrar_entrega` v2** (misma firma (uuid,jsonb), contrato nuevo: `{id_punto_entrega, telefono, nombre?}` — valida pedido PAGADO, punto activo de la sucursal del pedido, denormaliza nombre/ciudad/dirección del punto para la hoja de ruta, nombre→tbl_clientes). **Gap cerrado:** `fn_registrar_entrega` (m30) no estaba en 06_functions.sql/monolito — sincronizado ahora. **Seed de prueba:** transportadora "Expreso Bolívar" + 3 puntos en sucursal FER (retiro/punto local/envío Cbba) — `fn_listar_puntos_entrega` verificado (3 opciones ordenadas con transportadora resuelta). Matriz: filas 40/41/42 actualizadas + **filas WF-25-A/B/C añadidas** (deuda de trazabilidad de F5 cerrada). Pendiente: rewrite n8n WF-25-A/B + reglas WF-04 (encargo a Claude). Sin commit aún.

- **2026-09-08 (purga transaccional + nuevo producto para E2E)** — Purga autorizada y ejecutada en una transacción sobre el tenant FER: eliminados 49 pedidos, 6 envíos, 61 reservas, 11 entradas de lista, 23 comprobantes, 8 verificaciones, 18 seguimientos, 122 movimientos de inventario y 195 eventos WhatsApp de prueba. Se conservaron catálogo, clientes, créditos, usuarios, configuración, puntos de entrega y auditoría. Inventario FER reset a `stock_actual=0`/`stock_reservado=0`. Nuevo producto: **FERG01** "Producto Test Flujo Completo", precio Bs 200, producto `b967c460-39da-4ef4-83e9-cb56d68c65a7`, variante `5f51d435-9a31-4bc5-88bd-d5131b4aba77`, stock inicial 2 en sucursal FER. Verificación: pedidos/envíos/reservas/lista/comprobantes/verificaciones/movimientos/eventos = 0; FERG01 = 2 disponibles. Pendiente: ejecutar E2E reserva → lista → pago → puntos/logística y registrar resultados.

- **2026-09-08 (E2E FERG01 completo + m34 extensión logística + fixes H-19/H-20 + Momento 1 — cierre OBS-001)** — **E2E extendido validado de punta a punta** con FERG01 (Bs 200, 2 uds): reserva+QR unificado → SIN_STOCK (tenedor sintético) → lista pos #1 → notificación 🎯 → SI → reserva+QR → comprobante (OCR 32 Bs, D13 cajero confirma) → PAGADO + CONSUMO_VENTA −1 → nombre+2 apellidos → menú con **horario del punto** → opción transporte → **ciudad (Beni) → zona (Av. La Paz)** → confirmación → guía **EB-2026-004578** notificada → EN_RUTA → ENTREGADO ✓. **Hallazgos y fixes del día:** (1) **H-19** — QR desde aceptación con Producto/SKU vacíos: `Resolve Opportunity` de WF-14 ampliado (JOIN variante → sku/variant_name) y pasados a WF-20; (2) **mensaje QR duplicado** (OBS-002): WF-10 enviaba 2º mensaje textual → eliminado; WF-20 ahora envía UN mensaje con QR+Producto+SKU+Monto+Vigencia y **sin "Referencia:"** (petición del dueño); (3) **regresión WF-04**: chequeo de entrega pendiente estaba ANTES del IF SI/NO → SI entraba a logística; reordenado (SI/NO primero, luego entrega pendiente, luego ayuda) + eliminada rama doble; (4) **WF-25-B mutilado por hermes** (draft con 1 nodo; el activo seguía funcionando) → reconstruido completo por el orquestador con MCP n8n (flujo guiado por puntos); (5) **m34 `entrega_puntos_horarios_guia`**: `tbl_puntos_entrega.dias_atencion/horario_inicio/fin` (los definirá el cajero en Flutter), `tbl_clientes.apellido_paterno/materno` (fn_upsert_cliente v2 con 2 apellidos; nombre completo = nombres+paterno+materno), `tbl_envios.destino_ciudad/destino_zona`, `tbl_entrega_captura` (estado conversacional ciudad→zona, `fn_iniciar_captura_destino`/`fn_entrega_captura_estado`/`fn_procesar_captura_destino`), `fn_set_numero_guia` + rama guía en WF-25-C ("Tu paquete fue despachado vía X hacia ciudad, zona Y. Número de guía: Z"); (6) **duplicación de notificaciones de estado** — pg_net emite el webhook en PARES (timestamps idénticos; la guía por POST directo no se duplica) → **Regla 7 generalizada**: `tbl_notificaciones_envio` PK(id_envio,evento) + dedup en WF-25-C (primera vez→notifica; duplicado→200 y descarta); (7) **H-20** — nombre sobrescrito por teléfono: WF-21 llamaba `fn_upsert_cliente_json` (legacy m28, existente) con `p_nombre=phone` → migrado a `fn_upsert_cliente` con `p_nombre=''` (conserva nombre) + `fn_upsert_cliente_json` eliminada (m35); (8) **ambigüedad de sobrecarga** — m34 creó overload 7-args de fn_upsert_cliente y la 5-args vieja causaba `is not unique` en la llamada dinámica de WF-10 → dropped (m34b); (9) **Momento 1 (m35 `lista_pendiente_momento1`)**: WF-10 SIN_STOCK ya NO agrega directo a lista → `fn_pendiente_lista` + pregunta *"¿te avisamos cuando haya stock? SI/NO"*; WF-14 desambigua (oportunidad→Momento 2; pendiente→Momento 1): SI→`fn_aceptar_pendiente_lista` (guarda **YA_EN_LISTA** anti-duplicado, con posición)→WF-12 agrega y responde posición #N; NO→`fn_rechazar_pendiente_lista`. **OBS-001 COMPLETA (Momentos 1 y 2).** Pendiente: probar Momento 1 E2E (SIN_STOCK → SI/NO) en la próxima corrida; Flutter: definición de días/horarios por cajero + registro de guía (fns ya listas). Commit de cierre.

- **2026-09-08 (preparación migración n8n + P2 acumulados)** — Antes de migrar a la cuenta trial nueva (export/import JSON por el agotamiento del tier): **P2 cerrados**: (1) **H-17 hardening** — `Resolve Opportunity` de WF-14 ahora `ORDER BY fecha_notificacion DESC` (la oferta más reciente es la que el comprador responde); (2) **eventos stuck en PROCESANDO — causa raíz hallada y corregida**: "Prepare WF-80 Input" de WF-02 devolvía 0 items cuando no hay respuesta que enviar (p. ej. RESERVA_CREADA — el QR lo manda WF-20) → Send/Close nunca ejecutaban; fix: flag `enviar` + IF "¿Enviar WF-80?" (FALSE → Close Success directo) + salida de error de Send → Close Event Error1 (`34312844`); (3) **nombres/organización para export** — renombrados todos a formato `RSU | NN | Módulo | Función` (10→Ventas/Reserva SKU, 21→Pago/Comprobante Entrante, 22→"OCR Comprobante (Gemini)" — nombre GPT-4o obsoleto, 14→Lista/Aceptar Oportunidad, 25-A/B/C → A: Solicitar / B: Registrar / C: Notificar, 80 con espaciado uniforme); **WF-30 ARCHIVADO** (`[ARCHIVO]... supersedido por pg_cron m26` — NO exportar) · inventario final: **16 workflows activos**, 0 duplicados, ids en Matriz §0 reconstruida (WF-02 ID corregido a `NpZymtlm0CAaHWfW`). **Guía de migración**: exportar los 16 activos, importar en orden 80→22→24→23→21→20→10→03→04→02→12→13→14→25-A→25-B→25-C, republicar cada uno, re-referenciar IDs de executeWorkflow (WF-80 y los dispatch) y credenciales. Pendientes post-migración: E2E Momento 1 + `$vars` (META_*, SUPABASE_*, GEMINI_*, ENTREGA_TOKEN, LISTA_ESPERA_TOKEN) + credenciales (supabaseApi service_role, Postgres bypassrls, Gemini key). Commit de cierre de hito.

- **2026-09-08 (OBS-004 registrada — foto de guía/código de retiro, requiere app Flutter)** — Observación de campo del dueño (compras reales por TikTok): los negocios de paquetería entregan un **código de retiro** y las transportadoras una **guía**, ambos comunicados al comprador **como FOTO** (no texto). RSUELVO hoy registra `numero_guia` (texto) — insuficiente. **Alcance registrado como OBS-004** (ver `07-Control-de-Calidad/Observaciones-Usuario.md`): (1) app Flutter logística: captura de foto (cámara/galería) al entregar en paquetería (código de retiro) o transportadora (guía); (2) bucket privado `guias-envios`; (3) `tbl_envios.guia_foto_url`; (4) extender `fn_set_numero_guia` a PUNTO_LOCAL + foto; (5) notificación por WhatsApp como **imagen** vía WF-80 (patrón QR de pago). Impacto 🔴 — **implementación diferida a F4** (app Flutter con Antigravity + verificación en Android Studio; el prompt incluirá este flujo). Solo registrado hoy, sin implementación. Commit.

- **2026-09-09 (OBS-004 lado servidor listo + prompt Antigravity redactado)** — Pre-trabajo para F4: **migración 36 `guia_foto_obs004`**: `tbl_envios.guia_foto_url` + bucket privado `guias-envios` (policies authenticated INSERT/SELECT/UPDATE) + **`fn_registrar_guia(p_id_envio, p_numero_guia?, p_guia_foto_url?)`** (acepta ENVIO_TRANSPORTE y PUNTO_LOCAL/código de retiro, estados PREPARANDO/ASIGNADO/EN_RUTA, upsert texto+foto, notifica pg_net) · **WF-25-C** ahora envía la guía como **imagen con caption** cuando hay foto (`6f1d1b25`). **Usuarios de prueba creados** (Auth + tbl_usuarios + tbl_usuario_comercio→FER): `cajero@rsuelvo.test` (ROLE_TENANT_CASHIER), `repartidor@rsuelvo.test` (ROLE_LOGISTICS_AGENT), `dueno@rsuelvo.test` (ROLE_TENANT_ADMIN) — password `Test1234!`. **Prompt Antigravity redactado**: `01-Arquitectura/PROMPT-APP-FLUTTER-F4-Operativo.md` (contratos schema rsuelvo, fn_* firmas exactas, alcance v1 Operativo+Dashboard: Login/Verificaciones/Envíos+foto/Pedidos+botón WhatsApp wa.me/Dashboard mínimo/Productos lectura, fases con checkpoints flutter analyze, DoD checklist con datos reales, prohibiciones D11/RLS/no-migraciones). **Botón WhatsApp**: wa.me con el teléfono del comprador — chat humano, no viola D11 (el sistema no envía). Smoke reserva validado (H-20 fix publicado tras detectar draft sin publicar + responseFormat text). Sin commit de bitácora aún al escribir esta entrada.

- **2026-09-09 (migración n8n completada + URLs de triggers actualizadas + login app arreglado)** — **Migración n8n**: exports clasificados (16 canónicos en `WORKFLOWS EXTRAIDOS FINAL/rsuelvo-workflows`; 20 legacy/duplicados movidos a `_BORRAR/`); WF-25-C re-descargar (export desactualizado sin soporte de imagen); import + publicación + reconexión Meta realizados por el dueño. **Fix BD post-migración (crítico):** las 5 funciones con webhook hardcodeado (`fn_notifica_pedido_pagado`, `fn_notifica_envio_estado`, `fn_cron_expirar_y_notificar`, `fn_set_numero_guia`, `fn_registrar_guia`) apuntaban a `rsuelvo.app.n8n.cloud` (cuenta vieja) → confirmación desde la app funcionaba (pedido #55 PAGADO por "Cajero Prueba" ✓) pero el pg_net POST iba a la cuenta agotada (200 con página de trial, sin WhatsApp). Fix: swap de URL a `https://rsuelvotest.app.n8n.cloud` en las 5 (DO block con pg_get_functiondef+replace) + re-disparo manual del webhook entrega/request para el pedido #55. **Vault sync**: URLs actualizadas en 4 archivos SQL. **Login app arreglado**: error "Database error querying schema" de GoTrue — causa: seed SQL de usuarios con filas inválidas + sin `auth.identities` + constraint legacy `UNIQUE(provider_id,provider)` en auth.identities que permite solo 1 identidad email por proyecto (no modificable: owner supabase_auth_admin) → workaround: identidades con `provider_id=id` por usuario + UPDATE de filas replicando el patrón GoTrue (tokens vacíos, confirmado, raw_user_meta_data con sub/email). **Validado: 3 logins HTTP 200** (cajero/repartidor/dueno@rsuelvo.test). Pendiente: validar WhatsApp al comprador del pedido #55 + sync vault de fn m34/m35/m36 a 06_functions/monolito (deuda).

- **2026-09-09 (sesión F4: prompt ejecutado, app operativa + fixes fin de día)** — **App Flutter v1 construida por Antigravity** (login/roles, verificaciones con confirmar/rechazar, pedidos + botón WhatsApp, envíos + transiciones + **foto de guía/código OBS-004**, dashboard mínimo, productos lectura) — `flutter analyze` 0 issues. **Fixes del orquestador:** (1) **OBS-002 durable**: imagen del comprobante 401 — los `archivo_url` eran links `lookaside.fbsbx.com` que expiran en MINUTOS → WF-22 Ahora sube la imagen al bucket `comprobantes-pago` + **URL firmada 7 días** (nodos Subir a Storage/URL Firmada/Merge/Output Final) y WF-21 usa `p_archivo_url` = foto_url del OCR con fallback al link de Meta; (2) **login app**: "Database error querying schema" — GoTrue 500 (schema OK; simulé login vía curl): mis usuarios sembrados por SQL tenían filas inválidas + sin `auth.identities` (+ constraint legacy UNIQUE(provider_id,provider) incontournable → workaround provider_id=id por usuario) + UPDATE replicando el patrón GoTrue → **3 logins HTTP 200**; (3) **toasts UX final**: `mensajeResultadoEnvio`/`mensajeResultado` traducen códigos fn → humano; verificación ahora distingue PAGO_CONFIRMADO/YA_PROCESADO/RESERVA_VENCIDA; (4) **post-migración BD**: swap de URLs (rsuelvo.app.n8n.cloud → rsuelvotest) en las 5 funciones con webhook hardcodeado + re-disparo manual del pedido #55 ✓; (5) catálogo de pruebas sembrado (10 SKUs, FERC01/FERI01 en 0 para Momento 1, 5 puntos de entrega con 2 transportadoras). Matriz §0 migrada a los IDs de `rsuelvotest`. **Pendientes:** catálogo completo de pruebas en escritorio (tests A–E del catálogo), Flutter: pulir según uso real; sync vault fn m34/m35/m36 (deuda documentada); ganar confianza de trial (vigilar expiración de la cuenta n8n nueva). Commit de cierre de sesión.

- **2026-09-09 (OBS-005 implementada — destino en un solo mensaje)** — El comprador responde ciudad+zona en UN mensaje con formato `CIUDAD: <ciudad>, ZONA: <zona>` (tolerante a mayúsculas/espacios). **m37 `captura_destino_mensaje_unico`**: `fn_procesar_captura_destino` parsea ambos campos (strpos sobre texto en mayúsculas, limpia separadores ,/;/-), si falta formato → `FORMATO_INVALIDO` (n8n re-explica con ejemplo); `fn_entrega_captura_estado` → paso único `DESTINO`. **WF-25-B `rsuelvotest` (39f67d20)**: Msg Pide Ciudad muestra el formato+ejemplo; IF ahora valida `ENTREGA_REGISTRADA` (antes CIUDAD_CAPTURADA) y su rama FALSE (Msg Pide Zona re-purposado) re-envía el recordatorio de formato. Sin cambios de cableado estructural. Commit de cierre.

- **2026-09-09 (OBS-005 fix: ramas invertidas + captura zombi)** — Test E2E del flujo app→nombre halló 2 bugs: (1) **captura zombi** (`Sucre`/`zona NULL` del flujo de dos fases del mediodía) — `fn_entrega_captura_estado` matcheaba por teléfono sin exigir que la captura sea del pedido pendiente vigente → secuestró el mensaje del nombre (el comprador envió su nombre y la cadena lo interpretó como destino). Fix: borrada la fila zombi + guard en la fn (la captura solo cuenta si `id_pedido` = el pedido PAGADO-sin-envío MÁS RECIENTE del comprador — coherente con `fn_pedido_entrega_pendiente`). (2) **ramas invertidas de mi OBS-005**: cambié el IF a `ENTREGA_REGISTRADA` sin invertir las ramas → FORMATO_INVALIDO iba al mensaje de confirmación (con campos vacíos) y ENTREGA_REGISTRADA al recordatorio de formato. Fix: swap de conexiones + renombre "Msg Pide Zona"→"Msg Formato Invalido". `cb522c44`. **Sin daño en BD** (no se registró ningún envío con valores vacíos). El comprador debe reenviar su nombre para seguir el flujo. Commit.

- **2026-09-09 (OBS-005 final + fix de guard inexistente + simplificación "solo ciudad")** — Completada la prueba E2E del flujo app→nombre→puntos→ciudad: (1) **corrección de guard inexistente** — `fn_entrega_captura_estado` referenciaba `c.id_cliente` inexistente (el cliente va por pedido); reescrita con JOIN a pedidos+clientes y validada (`captura:false`); (2) el nombre del comprador pasó directo al menú ✓; (3) **OBS-005 aún más simple, por decisión del dueño**: la zona se ELIMINA del flujo — el comprador responde únicamente la ciudad/población (sin formato `CIUDAD:/ZONA:`); `fn_procesar_captura_destino` toma el texto completo como ciudad (120 chars max) y anula destino_zona; `fn_registrar_entrega v2.2` solo exige destino_ciudad; (4) el msg de menú cambia título a **"Donde enviamos tu pedido"** y la pregunta es "¿A qué ciudad o población se envía?" (`8d8fe482`). E2E restante: captura → envío → foto de guía desde la app. Commit de cierre.

- **2026-09-10 (prompt F6 + auditoría/rediseño para Codex + fixes de entorno)** — F6 de la app **completada por Antigravity (IDE)** según el dueño (pendiente verificación funcional). **Prompt F6** documentado en `01-Arquitectura/PROMPT-APP-FLUTTER-F6-Creditos-Config.md` (Créditos lectura, Configuración, Sucursales, Usuarios, **Puntos de Entrega con días/horarios** — cierra OBS-003 en la app). **BD preparada**: `GRANT INSERT/UPDATE/DELETE a authenticated` sobre `tbl_puntos_entrega` (faltaba; sin él F6-E fallaba). **agy (Antigravity CLI)**: instalado y funcional pero **cuota de cuenta agotada** (429 RESOURCE_EXHAUSTED, reset ~6 días) — diagnóstico en log; **MCPs mal configurados descubiertos**: `StitchMCP` con API key corrompida (se pegó un prompt en el header) y `n8n` apuntando a la cuenta vieja `rsuelvo2026` → deshabilitados Stitch/n8n/chrome-devtools/supabase en `agy mcp` para destrabar el arranque. **Prompt de auditoría + rediseño de marca para Codex** creado: `01-Arquitectura/PROMPT-CODEX-AUDITORIA-REDISENO-BRAND.md` — auditoría (marca/accesibilidad/UX/visual/técnico), sistema de diseño desde la paleta real del logotipo (`#2fac66→#38e0cc`, `#202020`, `#ededed`, isotipo de ruedas circulares → radios generosos/movimiento) e integración total de favicons/logos (login, app bars, estados vacíos, launcher icon por mipmaps, splash Android, web favicon) sin dependencias nuevas ni cambios de lógica. Commit de cierre.

- **2026-09-10 (auditoría + rediseño de marca de la app — Codex ✅)** — Codex ejecutó el prompt de auditoría/rediseño con resultado sobresaliente. **Entregables:** `AUDITORIA-App-RSUELVO.md` (traído al vault como `07-Control-de-Calidad/Auditoria-App-RSUELVO-2026-09-10.md` + evidencia en `evidencia-app-brand-2026-09-10/`), `lib/core/brand.dart` (gradiente/colores/radios/espacios/`BrandAssets`), `theme.dart` reescrito desde tokens, `shared/widgets/brand_widgets.dart` (`BrandMark/BrandPanel/BrandEmpty/BrandLoading`), assets en `assets/brand/` (PNG transparentes + SVG referencia + variantes `UI.png`), launcher por densidad, splash claro/oscuro (incl. Android 12+), favicon web; login hero con logo negativo sobre gradiente, favicon 28dp en nav, avatar, estados vacíos con marca. **Accesibilidad:** gradiente oscurecido con `brandDark` al 55% bajo texto blanco, **101 muestras de contraste AA** testeadas; tap targets 48dp; tooltips/semantics; textos flexibles 360dp y escala 130%. **WhatsApp `#25D366` intacto y excluido del gradiente.** **Verificación:** `flutter analyze` 0 issues · `flutter test` **18 tests passed** · `flutter build apk --debug` OK · APK instalado y navegado en **Samsung SM-A556E (Android 16)** — Dashboard/Pedidos/Envíos/Productos/Puntos de Entrega/Créditos/Configuración/Sucursales/Usuarios. **Limitación honesta:** el modelo de sesión no expone `nombre_comercio` → no se inventó (header usa usuario + resumen operativo); night mode solo en arranque nativo (sin emulador para prueba independiente). Commit vault.

- **2026-09-10 (H-21 · fix SKU base36 — reporte de Codex confirmado y corregido)** — Codex (CRUDs) reportó `"G" is not a valid hexadecimal digit` al crear variantes y **no tocó la BD** (correcto). Diagnóstico del orquestador: el trigger `fn_resolver_variante_tenant_sku` calculaba el máximo con `('x'||substr(sku,4,3))::bit(12)::int` → Postgres interpreta `x…` como **HEX** cuando el SKU es **base36**. Doble defecto: (a) cualquier sufijo con `G-Z` lanzaba excepción; (b) aun con A-F el máximo era erróneo (hex 0xA01=2561 vs base36 A01=12961) → riesgo de repetir/saltar SKU. **Migración 39 `fix_base36_sku`**: helper `fn_base36_a_int(text)` (IMMUTABLE) + uso en el trigger; la generación ya era base36 correcta. **Validado en transacción con rollback**: helper (A01=12961, K01=25921, G01=20737, Z99=45693, ZZZ=46655) · trigger normal → **FERK02** (siguiente tras K01) · forzado FERZ99 → siguiente **FERZ9A** (letras >F sin error). Vault sincronizado (06_functions + monolito con helper y expresión corregida + nuevo `39_fix_base36_sku.sql`). Commit.

- **2026-09-10 (Edge Function `invitar-usuario-comercio` desplegada + E2E ✅)** — Codex aprobó el enfoque híbrido y el orquestador construyó/desplegó la función vía Supabase MCP (`verify_jwt=true`, versión 1 ACTIVE). **Verificaciones previas que despejaron riesgos:** (a) **no existe trigger** de auto-provisión en `auth.users` → la EF inserta `tbl_usuarios` (punto 17 de la spec); (b) la constraint legacy `UNIQUE(provider_id, provider)` de `auth.identities` **no bloquea**: GoTrue crea las identidades con `provider_id = user_id` (verificado con el usuario de signup). **Prueba E2E real** (curl): login ADMIN → invitación CAJERO → **201** con `id_usuario` + `password_temporal` → **el invitado loguea con la contraseña temporal (LOGIN OK)** → repetido **409** → CAJERO intentando invitar **403**. Usuario demo limpiado (0 filas residuales). **Decisión v1 (acordada):** no envía correo — devuelve la contraseña temporal en la respuesta para que el ADMIN la comparta por canal seguro; el contrato queda listo para sumar Resend/SMTP en v2 sin cambios de API. **Contrato documentado** en `06-Integraciones/Edge-Function-invitar-usuario-comercio.md` (respuestas 201/400/401/403/409/500, UX del diálogo con botón Copiar, prohibición de `signUp()`/inserts directos). Codex integra ahora el formulario ADMIN en Flutter. Commit.

- **2026-09-10 (app: CRUDs + dashboards + invitación integrada — Codex ✅, contrato verificado)** — Codex completó: **CRUD Productos** (ADMIN; desactivar como principal, FK-safe) · **CRUD Variantes** (SKU solo lectura generado por trigger — ya funciona con m37…39; fila de inventario por sucursal; stock acumulado; el mensaje hexadecimal queda como fallback defensivo) · **Usuarios** (editar rol/sucursal/activo de vínculos + perfil propio; creación vía EF) · **Dashboards CAJERO y REPARTIDOR** integrados al diseño de marca (ruteo único `/dashboard` por rol + tab Inicio). **Integración de la Edge Function** `invitar-usuario-comercio`: diálogo ADMIN (email/nombre/apellido/teléfono/rol/sucursal con dropdown de sucursales existentes), invocación exclusiva a la EF, manejo 400/401/403/409/500 vía `FunctionException.details`, diálogo de éxito con contraseña temporal seleccionable + **botón COPIAR** + advertencia de canal seguro, refresco automático de la lista y **botón visible solo si `isAdmin`**. **Contrato verificado por el orquestador en código**: invoca solo `invitar-usuario-comercio`, lee `password_temporal`, sin `signUp()`, `tbl_usuarios` solo SELECT + UPDATE de perfil propio (coherente con `users_update_self`). Sin cambios de BD ni dependencias nuevas. `flutter analyze` 0 issues · `flutter test` 18 aprobados. Commits: app local + vault. Pendiente sugerido: verificación E2E desde la app de la invitación completa (crear → login del nuevo usuario), y v2 con Resend/SMTP.

- **2026-09-10 (migración 40 — catálogo por sucursal: overrides de variante + CAJERO habilitado)** — Decisión del dueño: **nombre/precio/activo de variante editables POR SUCURSAL** (no globales) + productos editables por ADMIN y CAJERO. **Migración 40**: (1) `tbl_variante_sucursal` (overrides nullable; NULL=hereda global) con PK(id_variante,id_sucursal), RLS (lectura por comercio; escritura ADMIN o CAJERO **de esa sucursal** vía helper `fn_puede_gestionar_catalogo`), triggers audit/updated_at, grants; (2) helper `fn_es_admin_o_cajero_comercio` (excluye repartidor) y política `products_manage` ampliada a ADMIN+CAJERO; (3) `fn_variante_efectiva(id_variante,id_sucursal)`; (4) `fn_resolver_variante_por_sku` v2 **con `p_id_sucursal`** (firma vieja de 2 args eliminada — regla anti-ambigüedad); (5) `fn_listar_variantes_sucursal(p_id_sucursal)` (efectivo + `tiene_override` + stock global/efectivo); (6) **`fn_crear_pedido_desde_reserva` usa el precio/nombre EFECTIVOS** de la sucursal de la reserva y los congela en el snapshot; (7) `fn_notificar_siguiente_lista_espera` devuelve `nombre`/`precio` efectivos + `tiempo_aceptacion_minutos` (para WF-13). **Validado E2E en transacción**: override 75 en FER sobre global 60 → efectiva 75 · sin override 60 · resolver con sucursal 75 · **pedido total y precio_unitario = 75** (camino de la plata ✓). **Edge Function `gestionar-variante-sucursal`** desplegada (CREATE de variante por CAJERO: valida rol/sucursal, SKU automático por trigger, inventario de su sucursal, compensación) — probada: 201 con SKU **FERK03** (continuación base36 real) · CAJERO otra sucursal **403** · REPARTIDOR **403** · variante de prueba limpiada. Vault sincronizado (`40_...sql` + 03_tables/06_functions/08_rls/monolito). **Pendientes:** n8n (WF-10 debe pasar `p_id_sucursal` al resolver; WF-13 usar los valores efectivos que devuelve la fn) y app (Leer: `fn_listar_variantes_sucursal`; Editar por sucursal: upsert en `tbl_variante_sucursal`; Crear: EF; global solo ADMIN).

- **2026-09-10 (sync vault m31/m34/m35/m36 — deuda saldada)** — Reconstrucción fiel desde cloud (pg_get_functiondef + catálogo): creados `34_entrega_captura_destino.sql` (tbl_entrega_captura + FKs/grants/triggers/política verificados + columnas m34 + fn_iniciar/estado/procesar con refinamientos 37/38 incluidos), `35_lista_pendiente_momento1.sql` (tbl_lista_pendiente + trío Momento 1 con guarda YA_EN_LISTA), `36_guia_foto.sql` (guia_foto_url + bucket guias-envios + 3 políticas storage + fn_registrar_guia final); espejados en 03_tables/06_functions/07_triggers/08_rls/11_storage/monolito + m31 (fn_rechazar_lista_espera) + cuerpo exacto de fn_notificar_siguiente_lista_espera. `13_fn_resolver...sql` marcado SUPERSEDED por m40. Nota: 37/38 fueron refinamientos de comportamiento (WF-25-B + cuerpo de fn_procesar) sin archivo propio; su estado final vive en 34.
- **2026-09-10 (Codex: app + n8n adaptados a catálogo por sucursal — VERIFICADO por orquestador)** — App: lectura vía `fn_listar_variantes_sucursal`, valores efectivos + `tiene_override`, selector de sucursal ADMIN / sucursal del vínculo para CAJERO, inventario por sucursal en pantalla de variantes, creación vía EF `gestionar-variante-sucursal`, ADMIN edita global / CAJERO edita override + "Usar valores globales" (NULL), CRUD productos ADMIN+CAJERO, SKU solo lectura, cambio de contraseña con `updateUser`. Verificado en código: `productos_repository.dart` (RPC L12, EF L33, upsert override L51), `variantes_screen.dart` usa el repository, `cambiar_password_dialog.dart` usa updateUser. `flutter analyze: 0 issues`, 18 tests OK (reportado por Codex). n8n: WF-10 Lookup ya envía `p_id_sucursal`; WF-13 Notify ya selecciona `nombre/precio/tiempo_aceptacion_minutos` y el mensaje los usa (verificado en JSON de ambos workflows). Cambios de app SIN COMMIT en working tree de `/home/nico/StudioProjects/rsuelvo` (6 archivos).

- **2026-09-10 (hallazgo filtro sucursal en inventario — NO es seed ni BD)** — Reporte: al cambiar de sucursal en inventario, todas las variantes aparecen en todas. Verificado: (1) el seed `10_seed.sql` jamás crea sucursales/variantes/inventario (solo roles/créditos/paquetes/plantillas) — las 3 sucursales + 11 variantes + filas de inventario son datos de pruebas; (2) la BD está sana: sin duplicados (1 fila por par sucursal×variante), Principal con stocks reales (0–15), Norte/Pampahasi solo con fila FERK02 en 0; (3) `fn_listar_variantes_sucursal` responde por diseño el catálogo GLOBAL (11 variantes) con stock coalescido a 0 — Norte devuelve todo en 0. Conclusión: es presentación en la app (los 0 aparecen como "existentes"). Encargo a Codex: en la vista de inventario filtrar/marcar `stock_actual==0` como "Sin stock en esta sucursal"; catálogo completo solo en vista de catálogo.

- **2026-09-10 (Codex: fix filtro sucursal en inventario — VERIFICADO en código)** — Implementado según encargo: `variantes_screen.dart` ordena con stock>0 primero, subtítulo "Sin stock en esta sucursal · Personalizada/Valor global" y badge "Sin stock" en gris cuando `stockActual==0`; eliminado el `ensureInventario` que creaba filas fantasma al abrir la pantalla. Además: `app_shell.dart` da al CAJERO la pestaña Productos y elimina el Dashboard duplicado del ADMIN. Alcance total del cambio Codex: 6 archivos (515+/138-), más `cambiar_password_dialog.dart` nuevo. Cambios de app aún SIN COMMIT en `/home/nico/StudioProjects/rsuelvo` (decisión del dueño).

- **2026-09-11 (revisión informe optimización + decisiones)** — Orquestador verificó la v1: modelo T/Q correcto (docs n8n: solo la raíz cuenta) y bugs reales (WF-24→WF-22, espacio en IF, texto vs OBS-003, media_url común en WF-20, precheck WF-12 anti-Regla 3). **P1 invalidado**: proponía CIUDAD+ZONA (texto m37) contra la decisión m38 (solo ciudad) — se mantiene solo-ciudad; OBS-005 corregida en vault (sección + historial). **m40 ausente del informe**. Acción: (1) OBS-005 corregida; (2) addendum v1→v2 en el informe; (3) `PROMPT-INTERVENCION-FIXES-FUNCIONALES.md` (F1–F5, F5 requiere migración 41); (4) `PROMPT-CODEX-OPTIMIZACION-EJECUCIONES-V2.md` (solo lectura, incorpora m38+m40).

- **2026-09-11 (informe optimización v2 — VERIFICADO)** — Codex actualizó solo el informe (+392/-0, v1 intacta): P1 invalidado (solo-ciudad, ahorro 0), m40 incorporada con firmas exactas (resolver 3 args, efectiva, listar, snapshot, notificar), F1–F5 remitidos al paquete de intervención, OBS-003/004/005 y lista "no tocar" actualizadas (+ítem m40). Hallazgo nuevo válido: `fn_aceptar_pendiente_lista` devuelve nombre/precio GLOBAL (no efectivo) — no usarlo como fuente de precio. workflows y BD intactos. Auditoría cerrada; pendientes: intervención F1–F5 (F5 espera migración 41) y propuestas P2–P8 contra Usage real.

- **2026-09-11 (migración 41 + prompt F1–F4)** — **Migración 41** `fn_agregar_lista_espera_v2` aplicada y validada en transacción (AGREGADO pos 1 → YA_EN_LISTA mismo id/pos → LISTA_LLENA activos 1/max 1; rollback; config intacta en 5): mismo lock FOR UPDATE que v1 + resultado jsonb + red unique_violation; v1 conservada hasta F5. Vault sincronizado (41 + 06_functions + monolito). Prompt `PROMPT-CODEX-INTERVENCION-F1-F4.md` listo (F1 con aclaración de passthrough espejando WF-13; F5 excluido). Pendiente: Codex ejecuta F1–F4 y reporta versiones.

- **2026-09-11 (F1/F2/F4 verificados y publicados; F3 decidido)** — Codex publicó: F1 (WF-24→WF-80 `7V6MIPuGbdx9s0lT`, mode each, ver. `a03891a8`) · F2 (sin espacio + Switch RESERVA_VENCIDA/YA_PROCESADO/ERROR_TECNICO con noOp silenciosos) · F4 (WF-20 `media_url` por comercio, ver. `c5eb50ca`). Verificado en JSON live por orquestador; Matriz §0 actualizada. Sin pruebas vivas (correcto: crean cobros/envían WhatsApp). **F3 no aplicado (bien pausado)**: tras F1, la rama TRUE de WF-24 duplica el aviso de WF-25-A. Decisión con evidencia: **25-A único emisor** — `trg_pedido_pagado_notifica` dispara a nivel BD (cubre modo auto Y manual D13, donde WF-24 ni corre) y 25-A ya envía "🎉 Pago confirmado + nombre" en producción (7 ejecuciones webhook OK); el envío de WF-24 estaba roto (→WF-22), así que quitarlo restaura el status quo de 1 mensaje. F3 redefinido: la rama TRUE de WF-24 termina sin enviar. Pendiente tu OK para despacharlo + F5 (tras migración 41 ya aplicada).

- **2026-09-11 (prompt F3+F5)** — F3 redefinido (25-A único emisor;era TRUE de WF-24 sin envío, sin borrar el Send compartido con la rama de vencida) y F5 especificado (WF-12 a `fn_agregar_lista_espera_v2`, textos intactos, v1 sin eliminar). Prompt `PROMPT-CODEX-INTERVENCION-F3-F5.md` listo para Codex.

- **2026-09-11 (F3+F5 verificados y publicados — intervención cerrada)** — Codex publicó: F3 (WF-24 ver. `a7f28e6b`: Build eliminado, TRUE→noOp, Send preservado para vencida) · F5 (WF-12 ver. `6d72176e`: RPC v2 parametrizada ($1–$4, sin interpolación) + Switch 3 reglas + fallback técnico; 3 textos verbatim; Output/WF-80 intactos; 13→9 nodos). Verificado en JSON live; Matriz §0 actualizada. Sin pruebas vivas (correcto). Intervención F1–F5 COMPLETA. Resta: eliminar `fn_agregar_lista_espera` v1 cuando F5 esté estable + casos E2E propuestos (confirmación auto/manual con aviso único; AGREGADO/YA_EN_LISTA/LLENA/técnico).

- **2026-09-11 (E2E Etapa A en curso)** — A1+A2 verificados: comprador 2 → FERC01 SIN_STOCK (WF-10 #478) → SI → WF-14 #485 → WF-12 #486 (F5 live) → `AGREGADO` pos 1, pendiente consumido. Hallazgo menor del dueño: el título decía "Producto ya reservado" en Momento 1 (sin reserva) → cambiado a **"Producto sin stock"** en WF-10 (ver. `aac5e27e` publicada). Nota: el test corre con comprador 2 (…1944), no el 1.

- **2026-09-11 (migración 42 + prompt WF-10 atajo)** — Decisión del dueño: reenviar SKU estando en lista responde directo sin pregunta SI/NO. **Migración 42** aplicada y validada (`fn_solicitar_reserva` devuelve `ya_en_lista/posicion/id_lista_espera` en ambos SIN_STOCK; buyer2→true pos 1, buyer1→false). Vault sincronizado (42 + 06_functions + monolito). Prompt `PROMPT-CODEX-WF10-YA-EN-LISTA.md` listo (IF + Build directo a Merge input 4; pregunta intacta para nuevos).

- **2026-09-11 (atajo ya_en_lista en WF-10 verificado; incidente de edición contenido)** — Codex implementó el atajo (IF `¿Ya en lista?` null-safe + Build directo a Merge idx 4, ver. `753e3616` publicada y activa). Verificado en JSON live: cableado, textos y extracción `ya_en_lista/posicion_lista` correctos. Incidente: el orquestador intentó limpiar un typo cosmético (`ála`, código muerto tras `return`, inofensivo y probado en A1/A3) y corrompió el borrador 4 veces seguidas al re-emitir el jsCode largo; **producción nunca afectada** (versión activa intacta). Resuelto con `restore_workflow_version` a `753e3616` (borrador `5260beda` idéntico al activo; no se publicó para no generar ruido). Lección: nodos Code largos solo los edita Codex/Claude con verificación de lectura posterior, o se usa restore ante la primera anomalía. El typo queda documentado para limpieza futura. Matriz §0 actualizada. Pendiente: re-test del dueño (FERC01 → respuesta directa sin pregunta).

- **2026-09-11 (bug atajo: id_comercio null → silent-drop en WF-80)** — El re-test del atajo no llegó al WhatsApp: WF-80 #508 recibió `id_comercio:null`, Resolve devolvió `[]` y terminó `success` sin enviar (último nodo: Resolve). Causa: `Build Respuesta Ya en lista` no propaga top-level. Intentos de fix del orquestador vía MCP fallaron (draft posiblemente sucio); prompt `PROMPT-CODEX-FIX-ATAJO-IDCOMERCIO.md` listo con diagnóstico, verificación previa obligatoria y spec exacta. Lección adicional: WF-80 marca success aunque no envíe por Resolve vacío (deuda observabilidad F7).

- **2026-09-11 (fix atajo id_comercio verificado E2E — atajo completo)** — Codex agregó las 3 asignaciones top-level (ver. `6cedb686` publicada). Prueba viva del dueño: FERC01 → respuesta directa recibida. Verificado en ejecución WF-80 #513: `id_comercio` poblado → Resolve devolvió `phone_number_id 1275143265687773` → payload construido → `lastNodeExecuted: Log Send` (vs #508 que moría en Resolve). Atajo ya_en_lista CERRADO de punta a punta. Matriz §0 actualizada. Resta Etapa A: A4 (LISTA_LLENA con comprador 1 + cupo temporal 1).

- **2026-09-11 (OBS-006 + migración 43)** — A4 verificado (buyer1 correctamente rechazado, sin fila; WF-12 #537). Obs del dueño: con lista llena, respuesta directa sin pregunta SI/NO → registrada OBS-006. **Migración 43** aplicada y validada (`lista_llena/activos/maximo` en ambos SIN_STOCK cuando no está en lista; buyer1→llena 1/1 con cupo temporal; cupo restaurado a 5). Vault sincronizado (43 + maestros + OBS-006). Prompt `PROMPT-CODEX-WF10-LISTA-LLENA.md` listo (IF en FALSE de ya_en_lista + Build con top-level obligatorio por lección #508).

- **2026-09-11 (rama lista llena en WF-10 verificado estructura)** — Codex publicó ver. `ab3d4121` (25 nodos): PG extrae `lista_llena`, IF `¿Lista llena?` en FALSE de ya_en_lista (triple condición null-safe), Build con texto exacto + top-level id_comercio/phone/provider (lección #508), Merge a 6 entradas. Verificado en JSON live; nada más tocado. Pendiente: prueba viva OBS-006 con cupo temporal 1 (comprador 1 → mensaje directo sin pregunta).

- **2026-09-11 (OBS-006 cerrada E2E — Etapa A COMPLETA)** — Comprador 1 con cupo 1 → mensaje directo recibido; verificado en WF-80 #546 (`id_comercio` poblado, Resolve OK, Log Send) + BD (sin fila ni pendiente para buyer1; buyer2 intacto #1). Cupo restaurado a 5. OBS-006 a ✅ Resuelta. Etapa A cerrada: A1/A2 (AGREGADO) + A3 (YA_EN_LISTA directo) + A4 (LISTA_LLENA) + OBS-006, todo validado en vivo.

- **2026-09-11 (B3: reserva OK, QR no renderiza — causa: falta archivo, no código)** — SI Momento 2 → WF-14 #558 → WF-20 #559 → WF-80 #560 con message-id Meta (aceptado) + BD perfecta (lista CONVERTIDO_RESERVA, reserva ACTIVA). Pero el QR no se ve: `media_url` por comercio (F4) devuelve **HTTP 400** — el archivo `qr-pagos/<id_comercio>/tienda.png` nunca se subió; la ruta común sí existe (200, 108KB). F4 expuso el gap: el código quedó bien, falta el dato. Sin políticas storage para qr-pagos (solo service_role/dashboard pueden subir). Acción: el dueño sube el QR vía dashboard; luego re-test con RESERVA_YA_EXISTENTE (reenvía QR). Deuda futura: subida de QR desde la app + validación de media_url (F7).
